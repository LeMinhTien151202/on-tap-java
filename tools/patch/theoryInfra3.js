// Lý thuyết CỐT LÕI: Observability, Resilience4j/Gateway, Flyway/MapStruct/Testcontainers — stack PayFlow.
// Dùng: node tools/theory_append.js tools/patch/theoryInfra3.js
module.exports = [
{
 topic: "Observability — Metrics, Logs, Traces",
 items: [
  {
   question: "Observability là gì? Ba trụ cột metrics/logs/traces khác nhau ra sao và dùng cái nào khi nào?",
   answer: "PHÂN BIỆT VỚI MONITORING — đây là câu mở đầu nên nói đúng:\n- MONITORING trả lời những câu hỏi bạn ĐÃ BIẾT TRƯỚC: 'CPU có vượt 80% không?', 'dịch vụ còn sống không?'. Bạn định nghĩa trước cái cần theo dõi.\n- OBSERVABILITY là khả năng trả lời những câu hỏi bạn CHƯA NGHĨ TỚI, chỉ dựa vào dữ liệu đã thu thập: 'vì sao riêng khách hàng ở miền Trung dùng VNPAY qua app di động lại chậm từ 14h hôm qua?'. Bạn không thể dựng dashboard trước cho câu hỏi đó.\nVới hệ thống monolith, đọc log là đủ. Với microservice, một request đi qua 8 dịch vụ và bạn không thể ghép chuyện bằng cách đọc 8 file log riêng — đó là lý do observability trở thành yêu cầu bắt buộc.\n\nBA TRỤ CỘT — mỗi cái mạnh ở một chỗ:\n\n1. METRICS — SỐ LIỆU TỔNG HỢP theo thời gian.\n- Rẻ nhất: lưu trữ gần như không đổi dù lưu lượng tăng gấp trăm lần.\n- Trả lời: 'CÓ VẤN ĐỀ KHÔNG?' và 'MỨC ĐỘ THẾ NÀO?'.\n- Điểm yếu: KHÔNG có ngữ cảnh của từng request. Biết p99 là 3 giây nhưng không biết request nào và vì sao.\n- Dùng cho: dashboard, cảnh báo, SLO.\n\n2. LOGS — SỰ KIỆN RỜI RẠC có dấu thời gian.\n- Nhiều ngữ cảnh nhất, nhưng ĐẮT NHẤT: dung lượng tăng tuyến tính theo lưu lượng.\n- Trả lời: 'CHÍNH XÁC ĐÃ XẢY RA CHUYỆN GÌ?'.\n- BẮT BUỘC PHẢI CÓ ĐỂ DÙNG ĐƯỢC: log CÓ CẤU TRÚC (JSON) và có TRACE ID. Log dạng văn bản tự do trong hệ phân tán gần như vô dụng vì không lọc và không tương quan được.\n\n3. TRACES — HÀNH TRÌNH của MỘT request qua nhiều dịch vụ.\n- Trả lời: 'THỜI GIAN ĐI ĐÂU HẾT?' và 'MẮT XÍCH NÀO HỎNG?'.\n- Đây là trụ cột giá trị nhất trong kiến trúc microservice và cũng là cái hay bị bỏ sót nhất.\n- Thường phải LẤY MẪU (sampling) vì lưu toàn bộ trace rất tốn.\n\nQUY TRÌNH ĐIỀU TRA SỰ CỐ CHUẨN — dùng cả ba theo đúng thứ tự:\n1. METRIC báo động: tỉ lệ lỗi của payment-service vọt lên 5%.\n2. Dashboard thu hẹp phạm vi: chỉ endpoint /payments/confirm, chỉ từ 14h05.\n3. TRACE cho thấy nút thắt: 92% thời gian nằm ở lời gọi tới VNPAY.\n4. LOG (lọc theo đúng trace id) cho thấy nguyên nhân cuối cùng: 'SSL handshake timeout'.\nKHÔNG có trụ cột nào thay thế được hai cái kia. Đội chỉ có log thì mất rất nhiều thời gian ở bước 2 và 3.\n\nCÁC TRỤ CỘT MỞ RỘNG hiện được nói tới nhiều: PROFILING liên tục (biết CPU tiêu ở hàm nào trên production, ví dụ Pyroscope) và EVENTS (sự kiện triển khai, thay đổi cấu hình — cực kỳ hữu ích vì phần lớn sự cố bắt đầu ngay sau một thay đổi).",
   examples: ["# Spring Boot: bật đủ ba trụ cột\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-actuator</artifactId>\n</dependency>\n<dependency>\n  <groupId>io.micrometer</groupId>\n  <artifactId>micrometer-registry-prometheus</artifactId>\n</dependency>\n<dependency>\n  <groupId>io.micrometer</groupId>\n  <artifactId>micrometer-tracing-bridge-otel</artifactId>\n</dependency>\n<dependency>\n  <groupId>io.opentelemetry</groupId>\n  <artifactId>opentelemetry-exporter-otlp</artifactId>\n</dependency>", "management:\n  endpoints:\n    web:\n      exposure:\n        include: health,info,prometheus,metrics\n  metrics:\n    tags:\n      application: ${spring.application.name}\n  tracing:\n    sampling:\n      probability: 0.1        # lấy mẫu 10%\n  otlp:\n    tracing:\n      endpoint: http://tempo:4318/v1/traces"]
  },
  {
   question: "Prometheus hoạt động thế nào? Mô hình pull, các loại metric và cardinality explosion",
   answer: "MÔ HÌNH PULL — Prometheus TỰ ĐI HỎI (scrape) endpoint /actuator/prometheus của từng ứng dụng theo chu kỳ (thường 15–30 giây), thay vì để ứng dụng đẩy số liệu tới.\nƯU ĐIỂM CỦA PULL: Prometheus tự biết mục tiêu nào chết (scrape thất bại là một tín hiệu miễn phí và rất đáng tin); dễ chạy thử bằng cách mở endpoint bằng trình duyệt; không lo ứng dụng làm ngập hệ thống giám sát.\nKHI NÀO CẦN PUSH: job ngắn hạn chạy xong là biến mất trước cả lần scrape kế tiếp — dùng Pushgateway cho trường hợp này, và CHỈ trường hợp này.\n\nBỐN LOẠI METRIC:\n1. COUNTER: CHỈ TĂNG (hoặc về 0 khi restart). Ví dụ: tổng số request, tổng số lỗi. GIÁ TRỊ TUYỆT ĐỐI CỦA COUNTER GẦN NHƯ VÔ NGHĨA — luôn dùng rate() để lấy tốc độ.\n2. GAUGE: tăng và giảm tự do. Ví dụ: số kết nối đang mở, kích thước hàng đợi, bộ nhớ đang dùng.\n3. HISTOGRAM: xếp quan sát vào các 'thùng' đã định trước. Tính phân vị Ở PHÍA SERVER bằng histogram_quantile, nên CỘNG GỘP ĐƯỢC giữa nhiều instance. Đây là lựa chọn đúng cho độ trễ.\n4. SUMMARY: tính phân vị NGAY TRONG ỨNG DỤNG. Chính xác hơn cho một instance nhưng KHÔNG CỘNG GỘP ĐƯỢC — không thể lấy trung bình các p99 của 10 pod để ra p99 toàn hệ thống. Đây là lý do hầu như luôn nên chọn histogram.\n\nMÔ HÌNH DỮ LIỆU: mỗi chuỗi thời gian được định danh bởi TÊN cộng TẬP NHÃN. http_requests_total{method=\"POST\", status=\"200\", uri=\"/payments\"} và cùng tên nhưng status=\"500\" là HAI CHUỖI KHÁC NHAU.\n\nCARDINALITY EXPLOSION — lỗi nghiêm trọng nhất và rất dễ mắc:\nSố chuỗi = TÍCH số giá trị khác nhau của mọi nhãn. Đặt userId làm nhãn với 1 triệu người dùng là tạo 1 triệu chuỗi cho mỗi tổ hợp nhãn khác — Prometheus sẽ hết bộ nhớ và sập.\nTUYỆT ĐỐI KHÔNG đặt làm nhãn: user id, order id, payment id, email, địa chỉ IP, URL có tham số động, mã trace, dấu thời gian.\nĐƯỢC PHÉP đặt làm nhãn: những thứ có TẬP GIÁ TRỊ HỮU HẠN VÀ NHỎ — tên dịch vụ, môi trường, mã trạng thái HTTP, tên endpoint đã chuẩn hóa (/payments/{id} chứ KHÔNG phải /payments/12345).\nQUY TẮC NHỚ ĐƠN GIẢN: cần ngữ cảnh chi tiết theo từng thực thể thì đó là việc của LOG hoặc TRACE, không phải của metric.\n\nPROMQL — vài mẫu phải thuộc:\n- rate(x_total[5m]): tốc độ mỗi giây, tính trung bình trong 5 phút. Dùng cho counter.\n- histogram_quantile(0.99, sum(rate(x_bucket[5m])) by (le)): p99. Chú ý phải sum theo nhãn le.\n- Tỉ lệ lỗi: sum(rate(x_total{status=~\"5..\"}[5m])) / sum(rate(x_total[5m])).\n- irate dùng cho biểu đồ nhạy với thay đổi ngắn; rate dùng cho cảnh báo vì mượt hơn.\n\nLƯU TRỮ: Prometheus lưu cục bộ và KHÔNG được thiết kế để giữ lâu dài hay chạy phân tán. Cần giữ nhiều tháng hoặc gộp nhiều cụm thì dùng Thanos, Cortex, hoặc Mimir.",
   examples: ["// metric nghiệp vụ — nhãn phải có TẬP GIÁ TRỊ NHỎ\n@Service\n@RequiredArgsConstructor\npublic class PaymentMetrics {\n    private final MeterRegistry registry;\n\n    public void ghiNhan(String method, String status, long ms) {\n        // ĐÚNG: method và status có ít giá trị\n        registry.counter(\"payflow_payment_total\",\n                \"method\", method, \"status\", status).increment();\n\n        registry.timer(\"payflow_payment_duration\",\n                \"method\", method).record(ms, TimeUnit.MILLISECONDS);\n\n        // SAI NGHIÊM TRỌNG: userId làm nhãn -> nổ cardinality\n        // registry.counter(\"payment_total\", \"userId\", userId).increment();\n    }\n}\n\n@Timed(value = \"payflow_confirm\", percentiles = {0.5, 0.95, 0.99})\npublic PaymentResult xacNhan(String id) { ... }", "# PromQL thường dùng\n# tốc độ request mỗi giây\nsum(rate(http_server_requests_seconds_count[5m])) by (uri)\n\n# tỉ lệ lỗi 5xx\nsum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m]))\n  / sum(rate(http_server_requests_seconds_count[5m]))\n\n# p99 độ trễ\nhistogram_quantile(0.99,\n  sum(rate(http_server_requests_seconds_bucket[5m])) by (le, uri))\n\n# đếm số chuỗi theo tên metric — tìm thủ phạm cardinality\ntopk(10, count by (__name__)({__name__=~\".+\"}))", "# quy tắc cảnh báo\ngroups:\n  - name: payflow\n    rules:\n      - alert: TiLeLoiCao\n        expr: |\n          sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m]))\n            / sum(rate(http_server_requests_seconds_count[5m])) > 0.05\n        for: 5m                # tránh báo động giả do gai nhọn\n        labels: { severity: critical }\n        annotations:\n          summary: \"Tỉ lệ lỗi 5xx vượt 5% trong 5 phút\""]
  },
  {
   question: "Distributed tracing: trace, span, context propagation và OpenTelemetry",
   answer: "MÔ HÌNH DỮ LIỆU:\n- TRACE: toàn bộ hành trình của MỘT request, có một TRACE ID duy nhất.\n- SPAN: một đơn vị công việc bên trong trace (một lời gọi HTTP, một truy vấn SQL, một lần publish Kafka). Mỗi span có span id, parent span id, thời điểm bắt đầu, thời lượng, và các thuộc tính.\n- Các span nối với nhau qua parent id tạo thành một CÂY. Giao diện dạng thác nước (waterfall) chính là cây đó vẽ theo trục thời gian, và nó cho thấy ngay thời gian đi đâu.\n\nCONTEXT PROPAGATION — phần cốt lõi và cũng là chỗ hay hỏng:\nĐể ghép được các span ở những tiến trình khác nhau, trace id phải ĐI THEO request qua ranh giới mạng. Chuẩn W3C Trace Context dùng header 'traceparent' với định dạng: phiên bản, trace id (32 ký tự hex), span id (16 ký tự hex), và cờ lấy mẫu.\nBA CHỖ THƯỜNG ĐỨT MẠCH TRACE:\n1. Gọi HTTP bằng client KHÔNG được cấu hình (dùng new RestTemplate() thay vì bean được Spring quản lý) — header không được chèn.\n2. Qua HÀNG ĐỢI: Kafka không tự mang context. Phải TỰ ĐƯA traceparent vào HEADERS của message ở phía producer và lấy ra ở phía consumer. Spring Kafka có hỗ trợ khi bật tracing, nhưng đây vẫn là chỗ đứt phổ biến nhất.\n3. Qua LUỒNG KHÁC: chuyển việc sang @Async hay ExecutorService làm mất context, vì nó nằm trong biến cục bộ luồng. Cần dùng ContextPropagatingExecutor hoặc bọc lại tác vụ.\n\nLẤY MẪU (sampling) — quyết định về chi phí:\n- HEAD-BASED: quyết định ngay ở request đầu tiên (ví dụ giữ 10%). Đơn giản, rẻ, nhưng CÓ THỂ BỎ SÓT chính cái trace bị lỗi mà bạn cần.\n- TAIL-BASED: giữ toàn bộ span rồi quyết định SAU KHI trace kết thúc — giữ mọi trace có lỗi hoặc chậm bất thường, và chỉ lấy mẫu nhỏ trong số trace bình thường. Đúng đắn hơn nhiều nhưng cần OTel Collector và nhiều bộ nhớ hơn.\n- Khuyến nghị: production dùng tail-based nếu làm được; nếu không thì head-based với tỉ lệ vừa phải cộng luôn giữ trace có lỗi.\n\nOPENTELEMETRY (OTel) — chuẩn thống nhất, đã thay thế OpenTracing và OpenCensus:\n- Gồm API (mã ứng dụng dùng), SDK (bản cài đặt), và giao thức OTLP.\n- HAI CÁCH TÍCH HỢP: JAVA AGENT (thêm -javaagent, KHÔNG CẦN SỬA MỘT DÒNG CODE, tự động trace được Spring, JDBC, Kafka, HTTP client) hoặc thư viện (kiểm soát chi tiết hơn). Với hệ thống có sẵn thì agent là cách vào nhanh nhất.\n- OTEL COLLECTOR là một tiến trình trung gian nên có: ứng dụng chỉ gửi tới collector, collector lo xử lý, lấy mẫu, và chuyển tiếp tới backend. Đổi backend từ Jaeger sang Tempo chỉ cần sửa cấu hình collector, không phải triển khai lại toàn bộ ứng dụng.\n\nLIÊN KẾT BA TRỤ CỘT: đưa trace id vào MỌI dòng log (qua MDC). Đó là thứ cho phép nhảy từ một span chậm trong Tempo sang đúng những dòng log của request đó trong Loki — và nó là tính năng có tỉ lệ giá trị trên công sức cao nhất trong toàn bộ mảng observability.",
   examples: ["# tích hợp bằng agent — KHÔNG sửa code\njava -javaagent:opentelemetry-javaagent.jar \\\n     -Dotel.service.name=payment-service \\\n     -Dotel.traces.exporter=otlp \\\n     -Dotel.exporter.otlp.endpoint=http://otel-collector:4317 \\\n     -Dotel.traces.sampler=parentbased_traceidratio \\\n     -Dotel.traces.sampler.arg=0.1 \\\n     -jar app.jar", "// span thủ công cho đoạn nghiệp vụ quan trọng\n@Service\n@RequiredArgsConstructor\npublic class PaymentService {\n    private final Tracer tracer;\n\n    public PaymentResult xuLy(PaymentRequest req) {\n        Span span = tracer.nextSpan().name(\"kiem-tra-rui-ro\").start();\n        try (var scope = tracer.withSpan(span)) {\n            span.tag(\"payment.method\", req.getMethod());\n            span.tag(\"payment.amount.range\", phanLoaiSoTien(req)); // KHÔNG ghi số tiền thật\n            return riskService.danhGia(req);\n        } catch (Exception e) {\n            span.error(e);\n            throw e;\n        } finally {\n            span.end();\n        }\n    }\n}\n// LƯU Ý: không đặt dữ liệu nhạy cảm (số thẻ, email) vào thuộc tính span", "// truyền context qua KAFKA — chỗ hay đứt mạch trace nhất\nProducerRecord<String, PaymentEvent> rec =\n        new ProducerRecord<>(\"payment-events\", key, event);\n\nW3CTraceContextPropagator.getInstance().inject(\n        Context.current(), rec.headers(),\n        (headers, k, v) -> headers.add(k, v.getBytes(UTF_8)));\n\nkafkaTemplate.send(rec);"]
  },
  {
   question: "Structured logging, log aggregation với Loki và cách gắn trace id vào log",
   answer: "VẤN ĐỀ GỐC: log dạng văn bản tự do trong hệ phân tán gần như không dùng được. Bạn không lọc được theo trường, không thống kê được, và không ghép được các dòng thuộc cùng một request nằm rải rác ở 8 dịch vụ.\n\nBA NGUYÊN TẮC BẮT BUỘC:\n\n1. LOG CÓ CẤU TRÚC (JSON): mỗi dòng là một đối tượng JSON có các trường rõ ràng. Nhờ đó truy vấn được kiểu 'mọi log có status=FAILED và method=VNPAY trong 10 phút qua'. Spring Boot 3.4+ có sẵn cấu trúc log JSON qua một thuộc tính cấu hình; trước đó dùng logstash-logback-encoder.\n\n2. TRACE ID TRONG MỌI DÒNG LOG: đây là điều giá trị nhất. Spring Boot với Micrometer Tracing tự đưa traceId và spanId vào MDC (Mapped Diagnostic Context — một Map gắn theo luồng), và mẫu log mặc định đã in chúng ra. Cần thêm ngữ cảnh nghiệp vụ thì tự đặt vào MDC, NHỚ dọn trong khối finally, vì luồng được tái sử dụng từ pool và giá trị cũ sẽ rò rỉ sang request khác.\n\n3. GHI LOG Ở MỨC PHÙ HỢP:\n- ERROR: cần con người can thiệp. Đừng lạm dụng — mọi thứ đều ERROR thì không gì là ERROR.\n- WARN: bất thường nhưng đã xử lý được.\n- INFO: sự kiện nghiệp vụ quan trọng (đã tạo payment, đã hoàn tiền).\n- DEBUG: chi tiết cho gỡ lỗi, tắt trên production.\nDùng THAM SỐ ({}) chứ đừng nối chuỗi — nối chuỗi thì việc dựng chuỗi vẫn xảy ra ngay cả khi mức log đó bị tắt.\n\nĐIỀU TUYỆT ĐỐI KHÔNG ĐƯỢC LOG (rất hay bị hỏi và rất hay bị vi phạm): mật khẩu, số thẻ đầy đủ, CVV, token, khóa API, thông tin định danh cá nhân. Với hệ thống thanh toán, đây là yêu cầu tuân thủ (PCI DSS), không phải lời khuyên. Số thẻ phải che, chỉ giữ 4 số cuối. Nên có bộ lọc che dữ liệu nhạy cảm ngay ở tầng logging để không phụ thuộc vào kỷ luật của từng lập trình viên.\n\nLOKI — cách tiếp cận khác với Elasticsearch:\n- Loki KHÔNG đánh index nội dung log, chỉ đánh index NHÃN (app, namespace, level). Nội dung được nén và lưu dạng khối.\n- Kết quả: rẻ hơn rất nhiều về lưu trữ và vận hành, nhưng truy vấn toàn văn chậm hơn Elasticsearch.\n- Nhãn của Loki chịu ĐÚNG vấn đề cardinality như Prometheus — đừng bao giờ đặt trace id hay user id làm NHÃN, hãy để chúng trong NỘI DUNG dòng log và lọc bằng LogQL.\n- Ngăn xếp Grafana thường gặp: Prometheus (metric) + Loki (log) + Tempo (trace), tất cả xem trong Grafana, có liên kết nhảy qua lại giữa ba loại.\n\nGOM LOG: ứng dụng chỉ nên GHI RA STDOUT (nguyên tắc 12-factor). Việc thu thập là của hạ tầng — Promtail, Fluent Bit, hay Grafana Alloy. Đừng để ứng dụng tự ghi file và tự xoay vòng trong container: file mất khi pod chết, và bạn phải giải quyết lại một bài toán đã có lời giải sẵn.",
   examples: ["# Spring Boot 3.4+: log JSON chỉ bằng một thuộc tính\nlogging:\n  structured:\n    format:\n      console: ecs        # hoặc logstash, gelf\n  level:\n    root: INFO\n    vn.payflow: DEBUG\n\n# mẫu log có trace id (mặc định khi bật Micrometer Tracing)\n# %d{ISO8601} %-5level [${spring.application.name},%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n", "// thêm ngữ cảnh nghiệp vụ vào MDC — NHỚ dọn trong finally\n@Component\npublic class MdcFilter extends OncePerRequestFilter {\n    @Override\n    protected void doFilterInternal(HttpServletRequest req, HttpServletResponse res,\n                                    FilterChain chain) throws IOException, ServletException {\n        try {\n            MDC.put(\"userId\", layUserId(req));\n            MDC.put(\"clientIp\", req.getRemoteAddr());\n            chain.doFilter(req, res);\n        } finally {\n            MDC.clear();      // BẮT BUỘC: luồng được tái sử dụng từ pool\n        }\n    }\n}", "// log ĐÚNG và SAI\nlog.info(\"Đã tạo payment {} số tiền {} qua {}\",\n         p.getId(), p.getAmount(), p.getMethod());          // ĐÚNG: tham số\n\nlog.info(\"Đã tạo payment \" + p.getId());                    // SAI: nối chuỗi\nlog.info(\"Thẻ {}\", card.getFullNumber());                   // VI PHẠM PCI DSS\nlog.info(\"Thẻ {}\", maskCard(card.getNumber()));             // ĐÚNG: ****1234\n\n// LogQL trong Loki\n// {app=\"payment-service\"} | json | level=\"ERROR\" | traceId=\"abc123\"\n// sum(rate({app=\"payment-service\"} | json | level=\"ERROR\" [5m]))"]
  },
  {
   question: "SLI, SLO, error budget và cách thiết kế cảnh báo không gây mệt mỏi",
   answer: "BA KHÁI NIỆM PHẢI TÁCH BẠCH:\n- SLI (Service Level Indicator): một PHÉP ĐO thực tế. Ví dụ: tỉ lệ request thành công, tỉ lệ request có độ trễ dưới 300ms.\n- SLO (Service Level Objective): MỤC TIÊU NỘI BỘ đặt ra cho SLI. Ví dụ: 99,9% request thành công trong cửa sổ 30 ngày.\n- SLA (Service Level Agreement): CAM KẾT VỚI KHÁCH HÀNG, có hậu quả pháp lý hoặc tài chính khi vi phạm. SLA luôn phải LỎNG HƠN SLO — bạn muốn tự phát hiện mình sắp hỏng trước khi khách hàng có quyền đòi bồi thường.\n\nERROR BUDGET — ý tưởng thay đổi cách làm việc:\nSLO 99,9% nghĩa là bạn ĐƯỢC PHÉP lỗi 0,1%. Trong 30 ngày, đó là khoảng 43 phút không khả dụng. Đó là NGÂN SÁCH LỖI.\nÝ nghĩa sâu hơn: 100% khả dụng KHÔNG PHẢI mục tiêu đúng — chi phí để đi từ 99,9% lên 99,99% thường lớn hơn nhiều so với giá trị nó mang lại, và người dùng thường không phân biệt được. Error budget biến việc này thành một quyết định kinh doanh có số liệu.\nDÙNG NGÂN SÁCH THẾ NÀO: còn nhiều ngân sách thì đội được phép triển khai nhanh, thử nghiệm mạnh. Tiêu gần hết thì ĐÓNG BĂNG tính năng mới và tập trung vào độ ổn định. Nhờ vậy cuộc tranh luận 'phát triển tính năng hay ổn định hệ thống' có một tiêu chí khách quan thay vì dựa vào cảm tính hay chức vụ.\n\nHAI PHƯƠNG PHÁP CHỌN CHỈ SỐ:\n- RED (cho DỊCH VỤ): Rate (số request/giây), Errors (tỉ lệ lỗi), Duration (phân bố độ trễ). Đây là bộ ba nên có cho mọi microservice.\n- USE (cho TÀI NGUYÊN): Utilization (mức sử dụng), Saturation (mức bão hòa, ví dụ độ dài hàng đợi), Errors. Dùng cho CPU, đĩa, connection pool.\nBỐN TÍN HIỆU VÀNG của Google SRE (Latency, Traffic, Errors, Saturation) về cơ bản là hợp nhất của hai bộ trên.\n\nTHIẾT KẾ CẢNH BÁO — quan trọng hơn người ta tưởng:\nCẢNH BÁO THEO TRIỆU CHỨNG, KHÔNG THEO NGUYÊN NHÂN. 'CPU 90%' không đáng đánh thức ai lúc 3 giờ sáng — nếu người dùng không bị ảnh hưởng thì đó không phải sự cố. 'Tỉ lệ lỗi vượt SLO' mới đáng.\nBA QUY TẮC:\n1. Mỗi cảnh báo phải HÀNH ĐỘNG ĐƯỢC. Không biết phải làm gì khi nhận cảnh báo thì đó là một dòng dashboard, không phải cảnh báo.\n2. Dùng 'for' để tránh gai nhọn tức thời, và MULTI-WINDOW MULTI-BURN-RATE cho cảnh báo dựa trên SLO: cảnh báo khẩn khi tốc độ đốt ngân sách nhanh (ví dụ nhanh gấp 14 lần trong 1 giờ) và cảnh báo thường khi đốt chậm nhưng kéo dài. Cách này vừa nhạy với sự cố lớn vừa không ồn ào với dao động nhỏ.\n3. MỆT MỎI VÌ CẢNH BÁO là rủi ro thật: khi 90% cảnh báo là báo động giả, người trực sẽ bỏ qua cả cái thứ 10 thật sự quan trọng. Xóa bớt cảnh báo là một hành động cải thiện độ tin cậy, không phải sự lười biếng.\n\nVỚI PAYFLOW: SLI quan trọng nhất không phải 'API còn sống' mà là 'tỉ lệ giao dịch thanh toán thành công' và 'thời gian từ lúc khách bấm thanh toán tới lúc nhận kết quả'. Chỉ số nghiệp vụ gần với trải nghiệm người dùng hơn chỉ số kỹ thuật.",
   examples: ["# cảnh báo dựa trên SLO: multi-window, multi-burn-rate\n# SLO 99.9% -> ngân sách lỗi 0.1%\ngroups:\n  - name: slo-payment\n    rules:\n      # đốt NHANH: hết ngân sách trong ~2 ngày -> gọi ngay\n      - alert: DotNganSachNhanh\n        expr: |\n          (\n            sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[1h]))\n              / sum(rate(http_server_requests_seconds_count[1h])) > 14.4 * 0.001\n          ) and (\n            sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m]))\n              / sum(rate(http_server_requests_seconds_count[5m])) > 14.4 * 0.001\n          )\n        labels: { severity: page }\n\n      # đốt CHẬM: tạo ticket, không gọi lúc 3h sáng\n      - alert: DotNganSachCham\n        expr: |\n          sum(rate(http_server_requests_seconds_count{status=~\"5..\"}[6h]))\n            / sum(rate(http_server_requests_seconds_count[6h])) > 3 * 0.001\n        for: 30m\n        labels: { severity: ticket }", "# bộ chỉ số RED cho mỗi dịch vụ\n# Rate\nsum(rate(http_server_requests_seconds_count[5m])) by (service)\n\n# Errors\nsum(rate(http_server_requests_seconds_count{status=~\"5..\"}[5m])) by (service)\n  / sum(rate(http_server_requests_seconds_count[5m])) by (service)\n\n# Duration (p50, p95, p99)\nhistogram_quantile(0.95,\n  sum(rate(http_server_requests_seconds_bucket[5m])) by (le, service))\n\n# SLI NGHIỆP VỤ — quan trọng nhất với PayFlow\nsum(rate(payflow_payment_total{status=\"SUCCESS\"}[30m]))\n  / sum(rate(payflow_payment_total[30m]))"]
  }
 ]
},
{
 topic: "Resilience4j, Gateway, Flyway, MapStruct, Testcontainers",
 items: [
  {
   question: "Circuit breaker hoạt động thế nào? Máy trạng thái, sliding window và cấu hình Resilience4j",
   answer: "VẤN ĐỀ CẦN GIẢI: gọi một dịch vụ đã hỏng là lãng phí kép — bạn chờ timeout (giữ luồng, giữ kết nối), rồi vẫn thất bại, và bạn còn tiếp tục dội tải lên một hệ thống đang cố phục hồi. Với microservice, điều này lan thành SỰ CỐ DÂY CHUYỀN: dịch vụ D chậm làm C hết luồng, C chậm làm B hết luồng, và cả hệ thống sập dù chỉ một thành phần hỏng.\n\nMÁY TRẠNG THÁI BA TRẠNG THÁI CHÍNH:\n- CLOSED (đóng — bình thường): mọi lời gọi đi qua, đồng thời ghi nhận kết quả. Tỉ lệ thất bại vượt ngưỡng thì chuyển sang OPEN.\n- OPEN (mở — đang ngắt): TỪ CHỐI NGAY LẬP TỨC mọi lời gọi mà không thử, ném CallNotPermittedException và chạy fallback. Điều này vừa bảo vệ dịch vụ kia vừa trả kết quả tức thì cho người dùng thay vì bắt họ chờ timeout.\n- HALF_OPEN (nửa mở — đang thăm dò): sau waitDurationInOpenState, cho một SỐ ÍT lời gọi thử đi qua. Thành công đủ tỉ lệ thì về CLOSED; vẫn thất bại thì quay lại OPEN.\nResilience4j còn có hai trạng thái đặc biệt: DISABLED (luôn cho qua) và FORCED_OPEN (luôn chặn) — hữu ích khi vận hành thủ công.\n\nHAI KIỂU SLIDING WINDOW:\n- COUNT_BASED: xét N lời gọi gần nhất. Dễ suy luận, phù hợp khi lưu lượng đều.\n- TIME_BASED: xét các lời gọi trong N giây gần nhất. Phù hợp hơn khi lưu lượng biến động mạnh.\nBẢO VỆ QUAN TRỌNG: minimumNumberOfCalls. Không có nó thì 1 lời gọi thất bại đầu tiên đã là 100% tỉ lệ lỗi và mạch mở ngay — cực kỳ nhạy và gây báo động giả.\n\nCẤU HÌNH HAY BỊ BỎ SÓT:\n- slowCallRateThreshold và slowCallDurationThreshold: coi lời gọi CHẬM như thất bại. Rất quan trọng, vì dịch vụ chậm thường nguy hiểm hơn dịch vụ chết hẳn — nó giữ luồng của bạn mà không báo lỗi.\n- ignoreExceptions: lỗi nghiệp vụ (ví dụ 'không đủ số dư', 404) KHÔNG được tính là lỗi hạ tầng. Nếu tính, mạch sẽ mở vì lý do sai hoàn toàn.\n- recordExceptions: chỉ tính các exception thật sự là dấu hiệu dịch vụ hỏng.\n\nTHIẾT KẾ FALLBACK — phần quan trọng nhất và hay bị làm qua loa:\nFallback phải CÓ NGHĨA VỀ NGHIỆP VỤ, không phải trả về null hay danh sách rỗng cho xong.\n- Đọc dữ liệu: trả về bản cache cũ, kèm dấu hiệu cho biết dữ liệu có thể không mới nhất.\n- Ghi dữ liệu quan trọng: KHÔNG được giả vờ thành công. Hãy đưa vào hàng đợi để xử lý sau và trả về trạng thái PENDING, hoặc từ chối rõ ràng.\n- Với thanh toán: TUYỆT ĐỐI không fallback bằng cách coi giao dịch là thành công. Trả PENDING và đối soát sau là cách đúng.\n\nTHỨ TỰ CÁC DECORATOR (mặc định của Spring Boot Resilience4j, từ ngoài vào trong): Retry → CircuitBreaker → RateLimiter → TimeLimiter → Bulkhead. Nghĩa là RETRY BỌC NGOÀI circuit breaker — mạch mở thì retry sẽ thấy CallNotPermittedException. Hiểu sai thứ tự này dẫn tới hành vi khác hẳn mong đợi, ví dụ retry dồn dập lên một dịch vụ đang hỏng.",
   examples: ["resilience4j:\n  circuitbreaker:\n    instances:\n      vnpay:\n        sliding-window-type: TIME_BASED\n        sliding-window-size: 60            # 60 giây gần nhất\n        minimum-number-of-calls: 20        # BẢO VỆ: đủ mẫu mới quyết định\n        failure-rate-threshold: 50\n        slow-call-rate-threshold: 80\n        slow-call-duration-threshold: 3s   # chậm cũng tính là hỏng\n        wait-duration-in-open-state: 30s\n        permitted-number-of-calls-in-half-open-state: 5\n        automatic-transition-from-open-to-half-open-enabled: true\n        record-exceptions:\n          - java.io.IOException\n          - java.util.concurrent.TimeoutException\n        ignore-exceptions:\n          - vn.payflow.exception.BusinessException   # lỗi nghiệp vụ KHÔNG tính", "@Service\npublic class VnpayClient {\n\n    @CircuitBreaker(name = \"vnpay\", fallbackMethod = \"duPhong\")\n    @Retry(name = \"vnpay\")\n    @TimeLimiter(name = \"vnpay\")\n    public CompletableFuture<PaymentResult> taoGiaoDich(PaymentRequest req) {\n        return CompletableFuture.supplyAsync(() -> restClient.post(req));\n    }\n\n    // fallback CÓ NGHĨA: không giả vờ thành công\n    private CompletableFuture<PaymentResult> duPhong(PaymentRequest req, Throwable t) {\n        log.warn(\"VNPAY không khả dụng, chuyển sang PENDING: {}\", t.toString());\n        outboxRepo.luuDeThuLaiSau(req);\n        return CompletableFuture.completedFuture(\n                PaymentResult.pending(\"Đang xử lý, kết quả sẽ được cập nhật\"));\n    }\n}", "// theo dõi trạng thái mạch\n@Bean\npublic RegistryEventConsumer<CircuitBreaker> theoDoi(MeterRegistry registry) {\n    return new RegistryEventConsumer<>() {\n        public void onEntryAddedEvent(EntryAddedEvent<CircuitBreaker> e) {\n            e.getAddedEntry().getEventPublisher()\n             .onStateTransition(ev -> log.warn(\"Mạch {} : {} -> {}\",\n                     ev.getCircuitBreakerName(),\n                     ev.getStateTransition().getFromState(),\n                     ev.getStateTransition().getToState()));\n        }\n    };\n}\n// chỉ số: resilience4j_circuitbreaker_state"]
  },
  {
   question: "Retry, bulkhead, rate limiter và time limiter: mỗi mẫu chống lại điều gì?",
   answer: "RESILIENCE4J CÓ NĂM MẪU, mỗi cái chống một dạng hỏng khác nhau. Dùng đúng cái cho đúng vấn đề mới có giá trị.\n\n1. RETRY — chống lỗi TẠM THỜI:\n- BA ĐIỀU KIỆN BẮT BUỘC trước khi retry: thao tác phải IDEMPOTENT (retry một lệnh trừ tiền không idempotent là trừ tiền hai lần); lỗi phải là loại có thể tự khỏi; và phải có GIỚI HẠN số lần.\n- EXPONENTIAL BACKOFF là bắt buộc: chờ 1s, 2s, 4s, 8s. Retry ngay lập tức chỉ làm dịch vụ đang quá tải càng quá tải.\n- JITTER (ngẫu nhiên hóa) cũng bắt buộc: không có nó thì 1000 client cùng thất bại sẽ cùng retry đúng một thời điểm — 'bầy đàn đồng bộ', và nó lặp lại mãi.\n- KHÔNG retry lỗi 4xx (trừ 429): request sai thì gửi lại 100 lần vẫn sai.\n- CHÚ Ý CHI PHÍ ẨN: retry NHÂN TẢI. Mỗi tầng retry 3 lần qua 3 tầng dịch vụ là 27 lần gọi thật. Retry chỉ nên đặt ở MỘT tầng, thường là tầng gần dịch vụ ngoài nhất.\n\n2. BULKHEAD (vách ngăn) — chống CẠN KIỆT TÀI NGUYÊN, tên lấy từ khoang kín của tàu thủy: một khoang thủng thì tàu vẫn nổi.\n- SEMAPHORE bulkhead: giới hạn số lời gọi ĐỒNG THỜI, chạy trên chính luồng gọi. Nhẹ, không đổi ngữ cảnh.\n- THREADPOOL bulkhead: chạy trên pool riêng, cách ly hoàn toàn. Nặng hơn nhưng cách ly triệt để.\n- GIÁ TRỊ THẬT: nếu dịch vụ chấm điểm rủi ro treo và bạn không có bulkhead, toàn bộ luồng của ứng dụng sẽ kẹt ở đó và cả ứng dụng chết — kể cả những endpoint không liên quan gì. Có bulkhead thì chỉ phần gọi rủi ro bị ảnh hưởng.\n\n3. RATE LIMITER — chống VƯỢT HẠN NGẠCH:\n- Giới hạn số lời gọi trong một chu kỳ. Dùng khi đối tác áp hạn mức (VNPAY cho 100 request/giây) và bạn muốn tự kiểm soát thay vì bị họ chặn.\n- Lưu ý: rate limiter của Resilience4j là CỤC BỘ TRONG MỘT JVM. Có 10 pod thì tổng thực tế là 10 lần giới hạn — cần giới hạn toàn cục thì phải dùng Redis.\n\n4. TIME LIMITER — chống CHỜ VÔ HẠN:\n- Đặt trần thời gian cho một lời gọi bất đồng bộ.\n- ĐIỂM QUAN TRỌNG NHẤT: hãy đặt timeout Ở MỌI LỜI GỌI MẠNG, ở cả tầng client HTTP. Không có timeout nghĩa là chờ mãi mãi, và đó là nguyên nhân gốc của rất nhiều sự cố dây chuyền. Nhiều thư viện HTTP mặc định KHÔNG có timeout — phải kiểm tra và đặt tay.\n\n5. CACHE — giảm số lời gọi, đã bàn ở phần Redis.\n\nKẾT HỢP CHÚNG: một lời gọi ra ngoài nghiêm túc thường có đủ timeout + circuit breaker + retry có backoff + bulkhead + fallback. Nghe có vẻ nhiều, nhưng mỗi cái chặn một dạng hỏng riêng và thiếu cái nào thì dạng hỏng đó vẫn xuyên qua.\n\nHAI MẪU LIÊN QUAN KHÔNG NẰM TRONG THƯ VIỆN: GRACEFUL DEGRADATION (tắt bớt tính năng phụ để giữ tính năng chính sống — ví dụ mất dịch vụ gợi ý thì vẫn cho thanh toán) và LOAD SHEDDING (chủ động từ chối một phần lưu lượng khi quá tải, vì phục vụ tốt 80% tốt hơn là làm 100% cùng chậm tới mức không dùng được).",
   examples: ["resilience4j:\n  retry:\n    instances:\n      vnpay:\n        max-attempts: 3\n        wait-duration: 1s\n        exponential-backoff-multiplier: 2\n        randomized-wait-factor: 0.5        # JITTER — bắt buộc\n        retry-exceptions:\n          - java.io.IOException\n          - java.util.concurrent.TimeoutException\n        ignore-exceptions:\n          - vn.payflow.exception.InvalidRequestException   # 4xx: không retry\n\n  bulkhead:\n    instances:\n      risk-service:\n        max-concurrent-calls: 20\n        max-wait-duration: 100ms\n\n  ratelimiter:\n    instances:\n      vnpay:\n        limit-for-period: 100\n        limit-refresh-period: 1s\n        timeout-duration: 200ms\n\n  timelimiter:\n    instances:\n      vnpay:\n        timeout-duration: 5s\n        cancel-running-future: true", "// LUÔN đặt timeout cho client HTTP — mặc định thường là VÔ HẠN\n@Bean\npublic RestClient vnpayClient(RestClient.Builder builder) {\n    var factory = new SimpleClientHttpRequestFactory();\n    factory.setConnectTimeout(Duration.ofSeconds(2));\n    factory.setReadTimeout(Duration.ofSeconds(5));\n    return builder\n            .baseUrl(\"https://sandbox.vnpayment.vn\")\n            .requestFactory(factory)\n            .build();\n}", "// RETRY NHÂN TẢI: chỉ đặt ở MỘT tầng\n// Gateway retry 3  x  BFF retry 3  x  Service retry 3  =  27 lần gọi thật\n// -> đặt retry ở tầng gần dịch vụ ngoài nhất, các tầng trên KHÔNG retry"]
  },
  {
   question: "Spring Cloud Gateway: kiến trúc reactive, route/predicate/filter và vai trò trong microservice",
   answer: "API GATEWAY LÀ ĐIỂM VÀO DUY NHẤT của hệ thống microservice. Không có nó, client phải biết địa chỉ của mọi dịch vụ, và mỗi dịch vụ phải tự cài lại xác thực, giới hạn tốc độ, ghi log, CORS.\n\nKIẾN TRÚC — điểm khác biệt cốt lõi:\nSpring Cloud Gateway xây trên SPRING WEBFLUX và NETTY, tức là BẤT ĐỒNG BỘ, KHÔNG CHẶN. Nó KHÔNG chạy trên Servlet/Tomcat (Zuul 1 thì có, và đó là lý do Zuul 1 bị thay thế).\nVÌ SAO ĐIỀU NÀY QUAN TRỌNG: công việc của gateway hầu như toàn là CHỜ I/O — nhận request rồi chờ dịch vụ phía sau trả lời. Với mô hình một luồng một request, 1000 request đồng thời cần 1000 luồng đang ngồi chờ. Với mô hình không chặn, một số ít luồng xử lý được hàng nghìn kết nối vì luồng được trả lại ngay khi bắt đầu chờ.\nHỆ QUẢ THỰC HÀNH RẤT QUAN TRỌNG: TUYỆT ĐỐI KHÔNG viết mã CHẶN trong filter của gateway (gọi JDBC, gọi RestTemplate đồng bộ, Thread.sleep). Chặn một luồng event loop là chặn hàng nghìn request. Cần gọi I/O trong filter thì phải dùng API reactive, hoặc chuyển sang scheduler riêng.\n\nBA KHÁI NIỆM:\n- ROUTE: một quy tắc định tuyến, gồm id, URI đích, danh sách predicate và danh sách filter.\n- PREDICATE: điều kiện KHỚP. Có sẵn theo Path, Method, Header, Query, Host, Cookie, thời gian (Before/After/Between), và cả RemoteAddr. Nhiều predicate được kết hợp bằng AND.\n- FILTER: thao tác trên request/response. Có filter theo route và global filter. Loại 'pre' chạy trước khi chuyển tiếp, loại 'post' chạy sau khi có phản hồi.\n\nCÁC FILTER CÓ SẴN HAY DÙNG: StripPrefix, RewritePath, AddRequestHeader, CircuitBreaker (tích hợp Resilience4j), RequestRateLimiter (dùng Redis, hoạt động TOÀN CỤC chứ không cục bộ như rate limiter của Resilience4j), Retry, và RemoveRequestHeader (dùng để xóa các header nội bộ trước khi chuyển tiếp).\n\nGATEWAY NÊN LÀM GÌ VÀ KHÔNG NÊN LÀM GÌ:\n- NÊN: định tuyến, chấm dứt TLS, xác thực token (kiểm tra chữ ký JWT và chuyển danh tính xuống dưới qua header), giới hạn tốc độ, CORS, ghi log và trace tập trung, cân bằng tải.\n- KHÔNG NÊN: chứa logic nghiệp vụ, tổng hợp dữ liệu phức tạp từ nhiều dịch vụ, truy cập cơ sở dữ liệu. Gateway phình logic sẽ trở thành một monolith mới — một điểm hỏng duy nhất mà mọi đội đều phải sửa và mọi thay đổi đều phải triển khai lại. Cần tổng hợp dữ liệu thì làm ở một tầng BFF riêng.\n\nBẢO MẬT: sau khi gateway xác thực, các dịch vụ bên trong vẫn KHÔNG NÊN tin tưởng mù quáng. Kẻ tấn công vào được mạng nội bộ có thể gọi thẳng dịch vụ và tự đặt header danh tính. Hoặc chuyển tiếp nguyên JWT để dịch vụ tự kiểm tra, hoặc dùng mTLS giữa các dịch vụ. Và LUÔN xóa các header danh tính nội bộ đến từ bên ngoài.",
   examples: ["spring:\n  cloud:\n    gateway:\n      default-filters:\n        - RemoveRequestHeader=X-Internal-User   # xóa header giả từ ngoài\n      routes:\n        - id: payment-service\n          uri: lb://payment-service\n          predicates:\n            - Path=/api/payments/**\n            - Method=GET,POST\n          filters:\n            - StripPrefix=1\n            - name: CircuitBreaker\n              args:\n                name: paymentCB\n                fallbackUri: forward:/fallback/payment\n            - name: RequestRateLimiter\n              args:\n                redis-rate-limiter.replenishRate: 100\n                redis-rate-limiter.burstCapacity: 200\n                key-resolver: \"#{@userKeyResolver}\"\n            - name: Retry\n              args:\n                retries: 2\n                methods: GET          # chỉ retry method idempotent\n                backoff:\n                  firstBackoff: 100ms\n                  maxBackoff: 1s", "// global filter: gắn trace id và ghi log — HOÀN TOÀN không chặn\n@Component\npublic class TracingFilter implements GlobalFilter, Ordered {\n    @Override\n    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {\n        long start = System.currentTimeMillis();\n        String path = exchange.getRequest().getPath().value();\n\n        return chain.filter(exchange)\n                .doFinally(sig -> log.info(\"{} {} - {}ms\",\n                        exchange.getRequest().getMethod(), path,\n                        System.currentTimeMillis() - start));\n        // KHÔNG dùng JDBC, RestTemplate, Thread.sleep ở đây\n    }\n    @Override public int getOrder() { return -1; }\n}", "// rate limit theo người dùng (toàn cục nhờ Redis)\n@Bean\npublic KeyResolver userKeyResolver() {\n    return exchange -> Mono.justOrEmpty(\n            exchange.getRequest().getHeaders().getFirst(\"X-User-Id\"))\n        .switchIfEmpty(Mono.just(\n            exchange.getRequest().getRemoteAddress().getAddress().getHostAddress()));\n}"]
  },
  {
   question: "Flyway và migration cơ sở dữ liệu: versioned/repeatable, checksum, và kỷ luật tương thích ngược",
   answer: "VẤN ĐỀ: schema database phải thay đổi theo code, phải áp dụng ĐÚNG THỨ TỰ và ĐÚNG MỘT LẦN trên mọi môi trường, và phải kiểm chứng được. Chạy tay file .sql là công thức của sự cố.\n\nFLYWAY HOẠT ĐỘNG THẾ NÀO:\n- Quét các file migration trong classpath (mặc định db/migration).\n- Duy trì bảng flyway_schema_history ghi lại: version, mô tả, CHECKSUM, ai chạy, khi nào, mất bao lâu, thành công hay không.\n- Khi khởi động, nó so sánh các file có sẵn với lịch sử và chỉ áp dụng những file CHƯA chạy, theo THỨ TỰ VERSION.\n\nBA LOẠI FILE:\n- VERSIONED (V1__ten.sql): chạy ĐÚNG MỘT LẦN, theo thứ tự version. Đây là loại chính.\n- REPEATABLE (R__ten.sql): chạy LẠI mỗi khi NỘI DUNG thay đổi (checksum đổi), và luôn chạy SAU tất cả versioned. Rất hợp cho view, function, stored procedure — vì bạn muốn sửa thẳng file thay vì tạo file mới mỗi lần.\n- UNDO (U1__ten.sql): chỉ có ở bản trả phí, và thực tế thì rollback tự động cho DDL hiếm khi đáng tin — cách thực dụng hơn là viết một migration tiến mới để sửa.\n\nCHECKSUM — quy tắc quan trọng nhất và cũng là lỗi phổ biến nhất:\nSỬA MỘT FILE MIGRATION ĐÃ CHẠY LÀ CẤM. Flyway sẽ phát hiện checksum lệch và TỪ CHỐI khởi động với lỗi 'Migration checksum mismatch'. Đây là tính năng, không phải phiền toái: nó bảo đảm mọi môi trường có cùng lịch sử. Cần sửa thì TẠO FILE MỚI.\n(flyway repair chỉ dùng khi thật sự cần chỉnh lại lịch sử, và phải hiểu rõ hậu quả.)\n\nBASELINE: áp Flyway lên database đã có sẵn dữ liệu thì dùng baseline-on-migrate và baseline-version, để Flyway coi trạng thái hiện tại là điểm khởi đầu và không cố chạy lại từ V1.\n\nKỶ LUẬT TƯƠNG THÍCH NGƯỢC — phần quan trọng nhất về mặt vận hành:\nTrong lúc rolling update, MÃ CŨ VÀ MÃ MỚI CHẠY ĐỒNG THỜI trên CÙNG một schema. Vì vậy mọi migration phải tương thích với cả hai phiên bản code.\nMẪU EXPAND–CONTRACT cho việc đổi tên cột, chia làm BA lần triển khai:\n1. EXPAND: thêm cột mới, cho phép NULL. Code ghi vào CẢ HAI cột, đọc từ cột cũ. Triển khai.\n2. MIGRATE: chép dữ liệu sang cột mới. Đổi code sang đọc cột mới. Triển khai.\n3. CONTRACT: khi chắc chắn không còn ai dùng cột cũ, xóa nó. Triển khai.\nĐổi tên cột trong MỘT lần triển khai sẽ làm sập mọi pod đang chạy mã cũ.\n\nBỐN QUY TẮC KHÁC:\n1. KHÔNG DÙNG hibernate.ddl-auto ngoài môi trường dev. Trên production phải là 'validate' hoặc 'none'. ddl-auto=update âm thầm thay đổi schema theo cách không kiểm soát và không thể tái hiện.\n2. Migration phải NGUYÊN TỬ nếu database hỗ trợ DDL trong transaction (PostgreSQL có, MySQL thì không).\n3. Trên bảng lớn, CREATE INDEX phải dùng CONCURRENTLY (và migration đó phải chạy ngoài transaction).\n4. Migration dữ liệu lớn nên chia lô, đừng UPDATE 50 triệu dòng trong một câu lệnh — nó khóa lâu và tạo khối lượng WAL khổng lồ.\n\nKIỂM THỬ MIGRATION: chạy chúng trong test tích hợp với Testcontainers. Đây là cách duy nhất để biết chắc chúng chạy được trước khi lên production.",
   examples: ["src/main/resources/db/migration/\n├── V1__tao_bang_accounts.sql\n├── V2__tao_bang_payments.sql\n├── V3__them_index_payment_status.sql\n├── V4__them_cot_payment_method.sql\n└── R__view_payment_summary.sql        # chạy lại khi nội dung đổi\n\n# cấu hình\nspring:\n  flyway:\n    enabled: true\n    locations: classpath:db/migration\n    baseline-on-migrate: true\n    baseline-version: 0\n    validate-on-migrate: true\n  jpa:\n    hibernate:\n      ddl-auto: validate      # KHÔNG BAO GIỜ dùng update trên production", "-- EXPAND-CONTRACT: đổi tên cột an toàn qua 3 lần triển khai\n\n-- V10: EXPAND — thêm cột mới, cho NULL\nALTER TABLE payments ADD COLUMN payment_method varchar(32);\n\n-- V11: MIGRATE — chép dữ liệu theo LÔ, không khóa cả bảng\nUPDATE payments SET payment_method = method\n WHERE payment_method IS NULL AND id BETWEEN 1 AND 100000;\n\n-- V12: CONTRACT — chỉ chạy khi chắc chắn không còn ai dùng cột cũ\nALTER TABLE payments DROP COLUMN method;", "-- index trên bảng lớn: KHÔNG khóa ghi\n-- migration này phải chạy NGOÀI transaction\nCREATE INDEX CONCURRENTLY IF NOT EXISTS idx_payments_status_created\n    ON payments (status, created_at DESC);", "// kiểm thử migration bằng Testcontainers\n@SpringBootTest\n@Testcontainers\nclass MigrationTest {\n    @Container\n    static PostgreSQLContainer<?> pg = new PostgreSQLContainer<>(\"postgres:16-alpine\");\n\n    @DynamicPropertySource\n    static void props(DynamicPropertyRegistry r) {\n        r.add(\"spring.datasource.url\", pg::getJdbcUrl);\n        r.add(\"spring.datasource.username\", pg::getUsername);\n        r.add(\"spring.datasource.password\", pg::getPassword);\n    }\n\n    @Test\n    void migrationChayDuocTuDau() {\n        // context khởi động thành công = mọi migration hợp lệ\n    }\n}"]
  },
  {
   question: "MapStruct và Testcontainers: vì sao dùng chúng thay vì cách thủ công?",
   answer: "MAPSTRUCT — SINH MÃ ÁNH XẠ LÚC BIÊN DỊCH:\nBài toán: chuyển Entity sang DTO và ngược lại. Viết tay thì dài dòng và dễ quên trường mới; dùng thư viện phản chiếu (ModelMapper, Dozer) thì chậm và lỗi chỉ lộ ra lúc chạy.\nMAPSTRUCT LÀ MỘT ANNOTATION PROCESSOR: nó SINH RA mã Java thuần lúc BIÊN DỊCH.\nBỐN LỢI ÍCH:\n1. HIỆU NĂNG BẰNG VIẾT TAY — mã sinh ra chỉ là getter/setter, không có phản chiếu.\n2. LỖI PHÁT HIỆN LÚC BIÊN DỊCH: đổi tên một trường ở entity mà quên cập nhật DTO thì BUILD HỎNG NGAY, thay vì gặp null lúc chạy trên production. Đặt unmappedTargetPolicy = ERROR để bắt buộc điều này.\n3. Mã sinh ra ĐỌC ĐƯỢC (nằm ở target/generated-sources), nên gỡ lỗi dễ.\n4. Tích hợp Spring bằng componentModel = \"spring\", tiêm như bean bình thường.\nĐIỂM THIẾT LẬP HAY SAI: khi dự án dùng Lombok, phải đặt lombok-mapstruct-binding và ĐÚNG THỨ TỰ annotation processor trong maven-compiler-plugin, nếu không MapStruct chạy trước Lombok, không thấy getter/setter, và sinh ra mapper rỗng — một lỗi im lặng rất khó chịu.\nBỐN TÍNH NĂNG NÊN BIẾT: @Mapping cho trường khác tên hoặc biểu thức, @Mapping(ignore = true) cho trường không ánh xạ (điển hình là id khi tạo mới), uses = {...} để tái sử dụng mapper khác, và @MappingTarget để cập nhật một đối tượng có sẵn thay vì tạo mới.\n\nTESTCONTAINERS — CHẠY PHỤ THUỘC THẬT TRONG DOCKER KHI TEST:\nBài toán: test tích hợp với H2 thay cho PostgreSQL là tự lừa dối. H2 không có kiểu JSONB, cú pháp khác, hành vi khóa khác, và không chạy được migration đặc thù Postgres. Test xanh mà production đỏ.\nTestcontainers khởi động container thật (PostgreSQL, Kafka, Redis, LocalStack, WireMock) trong vòng đời test và dọn dẹp tự động nhờ container Ryuk.\nBỐN ĐIỂM KỸ THUẬT:\n1. Container dùng CỔNG NGẪU NHIÊN để tránh xung đột — nên phải lấy URL động bằng @DynamicPropertySource, không được nối cứng cổng.\n2. @Container tĩnh (static) thì container dùng chung cho cả lớp test; không tĩnh thì tạo lại cho MỖI phương thức — chậm hơn rất nhiều. Gần như luôn nên dùng static.\n3. REUSE (testcontainers.reuse.enable=true trong ~/.testcontainers.properties): giữ container sống giữa các lần chạy test, tăng tốc rất mạnh khi phát triển cục bộ. Không nên bật trên CI.\n4. Từ Spring Boot 3.1 có @ServiceConnection: Spring TỰ cấu hình datasource/kafka/redis từ container, không cần viết @DynamicPropertySource thủ công nữa. Đây là cách nên dùng cho dự án mới.\n\nCHIẾN LƯỢC KIỂM THỬ TỔNG THỂ (kim tự tháp): nhiều unit test (nhanh, không có Spring context), một lớp vừa test tích hợp với Testcontainers (test repository, migration, luồng Kafka), và rất ít test đầu-cuối. Testcontainers chậm hơn unit test nên đừng dùng nó cho mọi thứ — nhưng cho tầng dữ liệu thì nó gần như là lựa chọn duy nhất đáng tin.",
   examples: ["@Mapper(componentModel = \"spring\",\n        unmappedTargetPolicy = ReportingPolicy.ERROR,   // quên trường -> BUILD HỎNG\n        uses = { AccountMapper.class })\npublic interface PaymentMapper {\n\n    @Mapping(target = \"id\", ignore = true)\n    @Mapping(target = \"status\", constant = \"PENDING\")\n    @Mapping(target = \"createdAt\", expression = \"java(java.time.Instant.now())\")\n    Payment toEntity(CreatePaymentRequest req);\n\n    @Mapping(source = \"account.name\", target = \"accountName\")\n    @Mapping(source = \"amount\", target = \"amountFormatted\", qualifiedByName = \"dinhDangTien\")\n    PaymentResponse toResponse(Payment p);\n\n    @Named(\"dinhDangTien\")\n    default String dinhDangTien(BigDecimal v) {\n        return v == null ? null : NumberFormat.getInstance(new Locale(\"vi\", \"VN\")).format(v);\n    }\n\n    // cập nhật đối tượng có sẵn thay vì tạo mới\n    void capNhat(@MappingTarget Payment dich, UpdatePaymentRequest nguon);\n}", "<!-- THỨ TỰ processor rất quan trọng khi dùng Lombok -->\n<plugin>\n  <artifactId>maven-compiler-plugin</artifactId>\n  <configuration>\n    <annotationProcessorPaths>\n      <path>\n        <groupId>org.projectlombok</groupId>\n        <artifactId>lombok</artifactId>\n      </path>\n      <path>\n        <groupId>org.projectlombok</groupId>\n        <artifactId>lombok-mapstruct-binding</artifactId>\n        <version>0.2.0</version>\n      </path>\n      <path>\n        <groupId>org.mapstruct</groupId>\n        <artifactId>mapstruct-processor</artifactId>\n        <version>1.6.3</version>\n      </path>\n    </annotationProcessorPaths>\n  </configuration>\n</plugin>", "// Spring Boot 3.1+: @ServiceConnection, không cần cấu hình thủ công\n@SpringBootTest\n@Testcontainers\nclass PaymentIntegrationTest {\n\n    @Container\n    @ServiceConnection\n    static PostgreSQLContainer<?> pg = new PostgreSQLContainer<>(\"postgres:16-alpine\");\n\n    @Container\n    @ServiceConnection\n    static KafkaContainer kafka =\n            new KafkaContainer(DockerImageName.parse(\"confluentinc/cp-kafka:7.6.0\"));\n\n    @Container\n    @ServiceConnection\n    static GenericContainer<?> redis =\n            new GenericContainer<>(\"redis:7-alpine\").withExposedPorts(6379);\n\n    @Autowired PaymentService service;\n\n    @Test\n    void taoPaymentVaPhatEvent() {\n        var kq = service.tao(new CreatePaymentRequest(1L, BigDecimal.valueOf(100_000)));\n        assertThat(kq.getStatus()).isEqualTo(PaymentStatus.PENDING);\n    }\n}\n\n// ~/.testcontainers.properties  (chỉ dùng khi phát triển cục bộ)\n// testcontainers.reuse.enable=true"]
  }
 ]
}
];
