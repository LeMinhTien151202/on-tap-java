// Lý thuyết Spring Data JPA (luồng bên trong + @Transactional) và Java hiện đại 8 -> 21.
// Dùng: node tools/theory_append.js tools/patch/theorySpringDataModern.js
module.exports = [
{
 topic: "Spring Data JPA — luồng bên trong & @Transactional",
 items: [
  {
   question: "Repository chỉ là một interface không có thân hàm — Spring Data JPA tạo ra cài đặt bằng cách nào?",
   answer: "Đây là câu hỏi 'bạn có thật sự hiểu framework hay chỉ dùng theo thói quen'.\n\nLUỒNG TỪ LÚC KHỞI ĐỘNG:\n1. @EnableJpaRepositories (Spring Boot bật tự động) kích hoạt JpaRepositoriesRegistrar, quét classpath tìm mọi interface kế thừa Repository.\n2. Với mỗi interface, nó đăng ký một JpaRepositoryFactoryBean — tức là một FactoryBean sinh ra bean thật.\n3. Lúc tạo bean, factory dựng một PROXY JDK động cài đặt interface của bạn.\n4. Proxy có một chuỗi interceptor. Mỗi lời gọi method được định tuyến theo thứ tự:\n   a. Method có @Query -> dùng chuỗi truy vấn khai báo sẵn.\n   b. Method có cài đặt riêng (custom fragment, tức interface XxxRepositoryCustom cùng lớp XxxRepositoryImpl) -> gọi thẳng vào đó.\n   c. Method là default method của interface -> gọi trực tiếp.\n   d. Còn lại -> PHÂN TÍCH TÊN METHOD (derived query).\n   e. Method của CrudRepository/JpaRepository -> ủy quyền cho SimpleJpaRepository, lớp cài đặt thật duy nhất.\n5. SimpleJpaRepository chỉ là một lớp bình thường bọc quanh EntityManager. Toàn bộ 'phép màu' nằm ở bước sinh proxy và phân tích tên.\n\nPHÂN TÍCH TÊN METHOD: PartTree tách tên thành phần chủ đề (find/read/get/count/exists/delete + Distinct + Top/First N) và phần vị ngữ (sau chữ By). Phần vị ngữ được tách theo And/Or, mỗi mảnh ánh xạ sang một thuộc tính cộng toán tử (Between, LessThan, Like, In, IsNull, IgnoreCase, OrderBy...). Nếu có thuộc tính không tồn tại, ứng dụng KHÔNG khởi động được — lỗi phát hiện ngay lúc chạy đầu tiên chứ không đợi tới lúc gọi, đó là một ưu điểm lớn.\n\nGIỚI HẠN CẦN BIẾT: tên method dài quá 3-4 điều kiện thì không ai đọc nổi — lúc đó chuyển sang @Query, Specification hoặc QueryDSL. Nhập nhằng khi tên thuộc tính lồng nhau thì dùng dấu gạch dưới để tách rõ (findByCustomer_Name).\n\nĐiểm ghi bàn: kể được tên SimpleJpaRepository và thứ tự ưu tiên định tuyến của proxy.",
   examples: ["public interface OrderRepository extends JpaRepository<Order, Long>, OrderRepositoryCustom {\n\n    // d) phân tích tên\n    List<Order> findTop10ByStatusAndAmountGreaterThanOrderByCreatedAtDesc(Status s, BigDecimal a);\n\n    // a) @Query — khi tên method sẽ quá dài\n    @Query(\"select o from Order o where o.customer.id = :cid and o.createdAt >= :from\")\n    List<Order> timTheoKhach(@Param(\"cid\") Long cid, @Param(\"from\") LocalDateTime from);\n\n    // c) default method\n    default Order layBatBuoc(Long id) {\n        return findById(id).orElseThrow(() -> new NotFoundException(id));\n    }\n}\n\n// b) cài đặt riêng: tên lớp phải là <TênInterface>Impl\ninterface OrderRepositoryCustom { List<Order> timPhucTap(SearchCriteria c); }\n\nclass OrderRepositoryCustomImpl implements OrderRepositoryCustom {\n    @PersistenceContext private EntityManager em;\n    public List<Order> timPhucTap(SearchCriteria c) { /* Criteria API */ }\n}"]
  },
  {
   question: "@Transactional hoạt động thế nào bên trong? Chuỗi từ proxy xuống DB gồm những gì?",
   answer: "@Transactional KHÔNG phải phép màu — nó là AOP dựa trên PROXY.\n\nLUỒNG ĐẦY ĐỦ:\n1. Lúc khởi động, InfrastructureAdvisorAutoProxyCreator phát hiện bean có @Transactional và bọc nó bằng proxy (JDK dynamic proxy nếu bean có interface, CGLIB nếu không — Spring Boot 2.x trở đi mặc định CGLIB).\n2. Khi bạn gọi method, lời gọi vào PROXY trước. TransactionInterceptor chặn lại.\n3. Interceptor đọc metadata (propagation, isolation, readOnly, timeout, rollbackFor) rồi hỏi PlatformTransactionManager.\n4. JpaTransactionManager mở transaction: lấy Connection từ DataSource, setAutoCommit(false), tạo EntityManager, rồi GẮN cặp này vào ThreadLocal của TransactionSynchronizationManager.\n5. Method thật chạy. Mọi repository trong cùng LUỒNG đó lấy được đúng EntityManager từ ThreadLocal nên tất cả nằm chung một transaction.\n6. Kết thúc: không lỗi -> flush persistence context rồi commit; có RuntimeException -> rollback.\n7. Gỡ khỏi ThreadLocal, đóng EntityManager, trả Connection về pool.\n\nQUY TẮC ROLLBACK MẶC ĐỊNH — nguồn của rất nhiều lỗi thật: Spring CHỈ tự rollback với RuntimeException và Error. Với CHECKED EXCEPTION (IOException, Exception tự định nghĩa kế thừa Exception) thì nó vẫn COMMIT. Muốn khác đi phải khai báo rollbackFor = Exception.class.\n\nBA BẪY LỚN CỦA MÔ HÌNH PROXY:\n1. SELF-INVOCATION: method này gọi method khác TRONG CÙNG bean bằng this — lời gọi không đi qua proxy nên @Transactional trên method được gọi HOÀN TOÀN VÔ TÁC DỤNG, im lặng không báo gì. Cách chữa: tách sang bean khác (sạch nhất), hoặc tiêm chính mình qua @Lazy, hoặc dùng AopContext.currentProxy().\n2. Method PRIVATE, final, hoặc static: proxy không ghi đè được nên annotation bị bỏ qua. Chỉ public (và không final) mới có tác dụng với proxy CGLIB.\n3. NUỐT EXCEPTION: bạn bắt exception bên trong và không ném lại -> Spring tưởng mọi thứ ổn -> COMMIT. Ngược lại, nếu một transaction NỘI đã đánh dấu rollback-only mà transaction ngoài lại cố commit thì bạn nhận UnexpectedRollbackException.\n\nĐiểm ghi bàn: nêu đúng ba bẫy và quy tắc 'checked exception không rollback'.",
   examples: ["// BẪY 1: self-invocation — @Transactional bị bỏ qua hoàn toàn\n@Service\nclass OrderService {\n    public void xuLy() { luu(); }              // this.luu() -> KHÔNG qua proxy\n    @Transactional public void luu() { ... }\n}\n// Sửa: tách sang bean khác\n@Service\nclass OrderService {\n    private final OrderTxService tx;\n    public void xuLy() { tx.luu(); }           // qua proxy -> có transaction\n}\n\n// BẪY 2: checked exception KHÔNG rollback\n@Transactional                          // IOException -> vẫn COMMIT (!)\npublic void a() throws IOException { ... }\n\n@Transactional(rollbackFor = Exception.class)   // ĐÚNG\npublic void b() throws IOException { ... }\n\n// BẪY 3: nuốt exception -> commit dữ liệu dở dang\n@Transactional\npublic void c() {\n    repo.save(x);\n    try { repo.save(y); } catch (Exception e) { log.error(\"bỏ qua\", e); }  // vẫn commit x\n}"]
  },
  {
   question: "Các mức propagation của @Transactional khác nhau thế nào? REQUIRES_NEW và NESTED dùng khi nào?",
   answer: "PROPAGATION quy định method sẽ làm gì khi ĐÃ CÓ một transaction đang chạy.\n\nBẢY MỨC:\n- REQUIRED (mặc định): có sẵn thì NHẬP VÀO, chưa có thì tạo mới. Vì cùng một transaction nên nếu bên trong ném lỗi thì TOÀN BỘ rollback — kể cả phần bên ngoài đã làm. Hơn 90% trường hợp dùng mức này.\n- REQUIRES_NEW: LUÔN tạo transaction MỚI, TẠM DỪNG transaction đang có. Hai transaction độc lập hoàn toàn: bên trong commit thì dữ liệu vẫn còn dù bên ngoài rollback. LƯU Ý QUAN TRỌNG: nó chiếm THÊM MỘT CONNECTION từ pool trong khi connection cũ vẫn bị giữ — lồng nhiều lớp REQUIRES_NEW dưới tải cao là công thức làm cạn pool và tự khóa chính mình. Ngoài ra hai transaction không thấy dữ liệu chưa commit của nhau, dễ gây khóa lẫn nhau nếu đụng cùng dòng.\n- NESTED: dùng SAVEPOINT của JDBC trong CÙNG một transaction. Bên trong hỏng thì chỉ rollback về savepoint, phần ngoài vẫn tiếp tục; nhưng nếu ngoài rollback thì trong cũng mất. Chỉ hoạt động với DataSourceTransactionManager và DB hỗ trợ savepoint — JpaTransactionManager KHÔNG hỗ trợ mặc định.\n- SUPPORTS: có thì tham gia, không thì chạy không transaction.\n- NOT_SUPPORTED: tạm dừng transaction hiện có, chạy ngoài transaction.\n- MANDATORY: bắt buộc phải có sẵn, không thì ném lỗi. Hữu ích để ràng buộc method chỉ được gọi trong ngữ cảnh transaction.\n- NEVER: bắt buộc KHÔNG được có transaction.\n\nCA DÙNG ĐIỂN HÌNH CỦA REQUIRES_NEW: ghi NHẬT KÝ KIỂM TOÁN hoặc lưu bản ghi lỗi — bạn muốn dòng log ở lại DB ngay cả khi nghiệp vụ chính rollback. Hoặc xử lý từng phần tử trong một lô, phần tử nào hỏng thì bỏ qua phần tử đó chứ không hủy cả lô.\n\nCẢNH BÁO KẾT HỢP: REQUIRES_NEW bên trong một vòng lặp lớn sẽ mở/đóng transaction hàng nghìn lần và mượn/trả connection liên tục — rất chậm. Với nhu cầu 'bỏ qua phần tử lỗi', thường nên gom lỗi lại rồi xử lý sau, hoặc dùng hàng đợi.\n\nĐiểm ghi bàn: nêu 'REQUIRES_NEW tốn thêm một connection' và điều kiện dùng được NESTED.",
   examples: ["// nhật ký vẫn còn dù nghiệp vụ chính rollback\n@Service\nclass AuditService {\n    @Transactional(propagation = Propagation.REQUIRES_NEW)\n    public void ghi(String su_kien) { auditRepo.save(new Audit(su_kien)); }\n}\n\n@Service\nclass PaymentService {\n    @Transactional\n    public void thanhToan(Order o) {\n        audit.ghi(\"BAT_DAU:\" + o.getId());   // transaction riêng, commit ngay\n        truTien(o);                          // ném lỗi -> chỉ phần này rollback\n    }\n}\n\n// ràng buộc: chỉ được gọi trong transaction\n@Transactional(propagation = Propagation.MANDATORY)\npublic void thaoTacNhayCam() { ... }\n\n// đọc báo cáo nặng, không cần transaction\n@Transactional(propagation = Propagation.NOT_SUPPORTED)\npublic List<Row> xuatBaoCao() { ... }"]
  },
  {
   question: "Khi nào dùng @Modifying? Vì sao truy vấn UPDATE/DELETE của JPQL lại gây dữ liệu lỗi thời?",
   answer: "@Modifying đánh dấu một @Query là câu thay đổi dữ liệu (UPDATE/DELETE/INSERT native). Thiếu nó, Spring Data sẽ cố chạy như một truy vấn SELECT và ném InvalidDataAccessApiUsageException.\n\nVẤN ĐỀ CỐT LÕI — TRUY VẤN HÀNG LOẠT ĐI VÒNG QUA PERSISTENCE CONTEXT: câu 'update Order o set o.status = ...' được dịch thẳng thành SQL và chạy trên DB. Hibernate KHÔNG hề biết những entity nào bị ảnh hưởng. Hệ quả kép:\n1. Các entity ĐANG nằm trong persistence context vẫn giữ giá trị CŨ. Đọc lại chúng trong cùng transaction sẽ ra dữ liệu lỗi thời, và tệ hơn: dirty checking lúc flush có thể GHI ĐÈ NGƯỢC giá trị cũ đè lên thay đổi bạn vừa làm.\n2. Nếu có thay đổi đang treo chưa flush, câu UPDATE hàng loạt có thể chạy TRƯỚC chúng và cho kết quả sai.\n\nHAI THAM SỐ GIẢI QUYẾT:\n- flushAutomatically = true: ép flush các thay đổi treo XUỐNG DB trước khi chạy câu này.\n- clearAutomatically = true: XÓA persistence context sau khi chạy, buộc mọi lần đọc sau phải lấy lại từ DB.\nVới truy vấn hàng loạt, gần như luôn nên bật cả hai.\n\nCÁC ĐIỂM KHÁC PHẢI NHỚ:\n- Method @Modifying BẮT BUỘC nằm trong @Transactional (không phải readOnly), nếu không sẽ nhận TransactionRequiredException.\n- Kiểu trả về là int (số dòng bị ảnh hưởng) hoặc void. Kiểm tra số dòng này là cách tốt để phát hiện 'cập nhật không trúng bản ghi nào'.\n- UPDATE hàng loạt KHÔNG kích hoạt các callback vòng đời (@PreUpdate, @PostUpdate), KHÔNG tăng cột @Version, và KHÔNG chạy cascade. Nếu nghiệp vụ dựa vào những thứ đó thì phải tự xử lý — muốn tăng version thì viết tường minh 'set o.version = o.version + 1'.\n- deleteAll() của Spring Data nạp từng entity rồi xóa từng cái (để chạy cascade và callback); deleteAllInBatch() chạy MỘT câu DELETE. Cái sau nhanh hơn nhiều nhưng bỏ qua cascade — chọn theo nhu cầu.\n\nĐiểm ghi bàn: giải thích được vì sao cần clearAutomatically, và nêu việc @Version không tự tăng.",
   examples: ["@Modifying(flushAutomatically = true, clearAutomatically = true)\n@Query(\"update Order o set o.status = :moi where o.status = :cu and o.createdAt < :truoc\")\nint hetHan(@Param(\"cu\") Status cu, @Param(\"moi\") Status moi, @Param(\"truoc\") LocalDateTime t);\n\n@Transactional\npublic void chay() {\n    int n = repo.hetHan(PENDING, EXPIRED, LocalDateTime.now().minusHours(2));\n    log.info(\"Đã hết hạn {} đơn\", n);   // luôn kiểm tra số dòng\n}\n\n// version KHÔNG tự tăng với update hàng loạt -> tự viết\n@Modifying\n@Query(\"update Order o set o.status = :s, o.version = o.version + 1 where o.id in :ids\")\nint capNhat(@Param(\"s\") Status s, @Param(\"ids\") List<Long> ids);\n\n// xóa: chậm nhưng đủ cascade  vs  nhanh nhưng bỏ qua cascade\nrepo.deleteAll(list);          // n câu DELETE + callback\nrepo.deleteAllInBatch(list);   // 1 câu DELETE"]
  },
  {
   question: "Projection trong Spring Data JPA có mấy loại? Vì sao nên trả DTO thay vì entity?",
   answer: "BA CÁCH LẤY DỮ LIỆU DẠNG THU GỌN:\n\n1. INTERFACE PROJECTION (đóng): khai báo một interface chỉ có các getter cần thiết. Spring Data sinh proxy và — điểm quan trọng — nó tối ưu câu SQL để CHỈ CHỌN các cột đó. Đây là cách gọn nhất.\nProjection MỞ dùng @Value với biểu thức SpEL thì NGƯỢC LẠI: Spring buộc phải nạp CẢ entity mới tính được biểu thức, nên mất hết lợi ích về SQL. Biết điều này mới dùng đúng.\n\n2. CLASS/DTO PROJECTION: dùng record hoặc lớp có constructor khớp tên tham số. Với @Query thì dùng biểu thức constructor 'select new com.app.OrderView(o.id, o.amount)'. Rõ ràng, kiểm tra được lúc biên dịch (nếu dùng record thì càng gọn).\n\n3. DYNAMIC PROJECTION: khai báo method generic <T> List<T> findByStatus(Status s, Class<T> type) — cùng một method dùng lại cho nhiều kiểu trả về khác nhau.\n\nVÌ SAO KHÔNG TRẢ ENTITY RA API — năm lý do:\n1. LỘ DỮ LIỆU: mọi field đều thành JSON, kể cả passwordHash, nội bộ, ghi chú. Thêm một cột vào DB là vô tình lộ luôn ra API.\n2. LAZY VÀ VÒNG LẶP: Jackson chạm vào quan hệ lazy -> LazyInitializationException (nếu đã tắt OSIV) hoặc bắn hàng loạt truy vấn ngầm; quan hệ hai chiều thì serialize vòng vô tận.\n3. GẮN CHẶT HỢP ĐỒNG API VỚI LƯỢC ĐỒ DB: đổi tên cột là vỡ API của khách hàng.\n4. NẶNG: nạp cả entity vào persistence context, chịu chi phí chụp ảnh và dirty checking, trong khi bạn chỉ cần 3 cột.\n5. ĐẦU VÀO CÒN NGUY HIỂM HƠN: nhận thẳng entity từ request body cho phép người dùng gán bất kỳ trường nào, kể cả id, role, số dư — lỗ hổng mass assignment kinh điển.\n\nQUY TẮC THỰC HÀNH: entity chỉ sống trong tầng persistence; service map sang DTO ngay trong transaction; controller chỉ biết DTO. Với API đọc nhiều, truy vấn thẳng ra projection thường nhanh hơn vài lần vì cắt được cả số cột lẫn chi phí quản lý entity.\n\nĐiểm ghi bàn: nêu khác biệt giữa projection đóng và mở, và nêu lỗ hổng mass assignment.",
   examples: ["// 1) interface projection — SQL chỉ chọn 2 cột\npublic interface OrderSummary {\n    Long getId();\n    BigDecimal getAmount();\n    String getCustomerName();          // đi sâu vào quan hệ vẫn được\n}\nList<OrderSummary> findByStatus(Status s);\n\n// projection MỞ: tiện nhưng nạp cả entity\npublic interface OrderOpen {\n    @Value(\"#{target.customer.name + ' - ' + target.amount}\")\n    String getMoTa();\n}\n\n// 2) DTO bằng record + biểu thức constructor\npublic record OrderView(Long id, BigDecimal amount, String customerName) {}\n\n@Query(\"select new com.app.OrderView(o.id, o.amount, c.name) \" +\n       \"from Order o join o.customer c where o.status = :s\")\nList<OrderView> views(@Param(\"s\") Status s);\n\n// 3) dynamic projection — một method, nhiều kiểu trả về\n<T> List<T> findByStatus(Status s, Class<T> type);\nrepo.findByStatus(PAID, OrderSummary.class);\nrepo.findByStatus(PAID, OrderView.class);"]
  },
  {
   question: "Phân trang trong Spring Data JPA: Page, Slice và keyset pagination khác nhau thế nào?",
   answer: "Page<T>: trả về dữ liệu trang hiện tại CỘNG tổng số bản ghi. Để có tổng số, Spring Data chạy THÊM một câu 'select count(*)'. Với bảng hàng triệu dòng và điều kiện lọc phức tạp, câu count này thường ĐẮT HƠN cả câu lấy dữ liệu.\n\nSlice<T>: KHÔNG chạy count. Nó lấy pageSize + 1 bản ghi để biết 'còn trang sau không' rồi bỏ bản ghi thừa. Rẻ hơn hẳn. Dùng cho giao diện cuộn vô tận hoặc nút 'Xem thêm' — nơi bạn không cần hiển thị tổng số trang.\n\nList<T> với Pageable: chỉ lấy dữ liệu, không count, không kiểm tra trang sau. Nhẹ nhất.\n\nVẤN ĐỀ CỐT LÕI CỦA OFFSET PAGINATION: câu 'LIMIT 20 OFFSET 200000' buộc DB phải quét và BỎ QUA 200.000 dòng trước khi trả về 20 dòng. Càng vào sâu càng chậm, thời gian tăng tuyến tính theo số trang. Ngoài ra nếu có bản ghi mới được chèn giữa lúc người dùng lật trang thì các dòng bị TRÙNG hoặc BỊ NHẢY QUA.\n\nKEYSET PAGINATION (còn gọi seek method) — giải pháp đúng cho dữ liệu lớn: thay vì đếm offset, ta ghi nhớ giá trị của bản ghi CUỐI trang trước rồi truy vấn 'WHERE (created_at, id) < (:lastAt, :lastId) ORDER BY created_at DESC, id DESC LIMIT 20'. DB dùng chỉ mục nhảy thẳng tới vị trí cần, thời gian KHÔNG ĐỔI dù ở trang thứ mấy, và không bị trùng/nhảy dòng. Điều kiện: phải sắp theo một khóa DUY NHẤT và ỔN ĐỊNH (thường ghép thêm id để phá hòa), và bạn mất khả năng nhảy tới 'trang thứ 500' — đổi lại đó cũng là thứ gần như không ai dùng.\n\nHAI LƯU Ý QUAN TRỌNG KHÁC:\n1. LUÔN có ORDER BY xác định khi phân trang. Không có nó, DB không cam kết thứ tự giữa các lần chạy và kết quả có thể lộn xộn.\n2. Phân trang KHÔNG đi cùng JOIN FETCH collection (xem lại phần N+1) — nếu cần thì dùng chiến lược hai bước hoặc @BatchSize.\n3. Nhận Pageable thẳng từ request thì phải GIỚI HẠN kích thước trang, nếu không ai đó gọi size=1000000 là hạ được server. Đặt spring.data.web.pageable.max-page-size.\n\nĐiểm ghi bàn: giải thích được vì sao OFFSET lớn thì chậm và trình bày được keyset pagination.",
   examples: ["// Page: có tổng số, tốn thêm câu count\nPage<Order> page = repo.findByStatus(PAID, PageRequest.of(0, 20, Sort.by(\"createdAt\").descending()));\npage.getTotalElements();\n\n// Slice: cuộn vô tận, không count\nSlice<Order> slice = repo.findByStatusOrderByCreatedAtDesc(PAID, PageRequest.of(0, 20));\nslice.hasNext();\n\n// Keyset: nhanh đều ở mọi trang\n@Query(\"\"\"\n       select o from Order o\n       where o.status = :s\n         and (o.createdAt < :lastAt or (o.createdAt = :lastAt and o.id < :lastId))\n       order by o.createdAt desc, o.id desc\n       \"\"\")\nList<Order> trangTiep(@Param(\"s\") Status s,\n                      @Param(\"lastAt\") LocalDateTime lastAt,\n                      @Param(\"lastId\") Long lastId,\n                      Pageable limit);\n\n# chặn kích thước trang do client gửi lên\nspring.data.web.pageable.max-page-size=100\nspring.data.web.pageable.default-page-size=20"]
  },
  {
   question: "Specification, Criteria API và QueryDSL dùng khi nào? Xây bộ lọc động thế nào cho sạch?",
   answer: "BÀI TOÁN: màn hình tìm kiếm có 8 ô lọc, người dùng điền ô nào cũng được. Viết @Query cố định thì phải sinh 2^8 tổ hợp; ghép chuỗi SQL thì mở cửa cho SQL injection và khó bảo trì.\n\nBA CÔNG CỤ:\n\n1. CRITERIA API (chuẩn JPA): dựng cây truy vấn bằng object — CriteriaBuilder, CriteriaQuery, Root, Predicate. An toàn kiểu, không ghép chuỗi. Nhược điểm nổi tiếng là DÀI DÒNG và khó đọc.\n\n2. SPECIFICATION (Spring Data): lớp bọc mỏng quanh Criteria API, biến mỗi điều kiện thành một object Specification ghép được bằng and/or/not. Repository chỉ cần kế thừa JpaSpecificationExecutor. Đây là lựa chọn cân bằng nhất khi bạn không muốn thêm thư viện ngoài: mỗi tiêu chí là một method static nhỏ, tái dùng và test riêng được.\n\n3. QUERYDSL: sinh sẵn các lớp Q từ entity lúc biên dịch, cho cú pháp gần giống SQL nhưng an toàn kiểu và tự động hoàn thành trong IDE. Dễ đọc nhất trong ba cái. Cái giá là phải cấu hình annotation processor và có thêm bước sinh mã.\n\nMẸO VIẾT SẠCH: mỗi tiêu chí trả về null khi tham số rỗng, rồi dùng Specification.allOf hoặc gộp bằng Stream.filter(Objects::nonNull) — như vậy không cần một dòng if nào trong phần lắp ghép.\n\nCÁC ĐIỂM CẦN CẨN THẬN:\n- Vẫn phải kiểm soát TRƯỜNG SẮP XẾP theo danh sách trắng — Sort nhận từ client mà truyền thẳng vào có thể sắp theo trường bạn không muốn lộ, hoặc gây quét bảng.\n- Specification kết hợp phân trang thì vẫn dính vấn đề JOIN FETCH đã nói ở phần N+1.\n- Dùng join trong Specification phải cẩn thận nhân dòng khi quan hệ là một-nhiều — thêm distinct hoặc chuyển sang truy vấn hai bước.\n- Với truy vấn báo cáo cực phức tạp, đừng cố ép qua JPA — dùng thẳng JdbcClient/native query sẽ ngắn hơn và nhanh hơn nhiều.\n\nĐiểm ghi bàn: trình bày mẫu 'tiêu chí trả null khi rỗng rồi gộp' — nó cho thấy bạn từng làm màn hình lọc thật.",
   examples: ["public interface OrderRepository\n        extends JpaRepository<Order, Long>, JpaSpecificationExecutor<Order> {}\n\nfinal class OrderSpecs {\n    static Specification<Order> trangThai(Status s) {\n        return s == null ? null : (root, q, cb) -> cb.equal(root.get(\"status\"), s);\n    }\n    static Specification<Order> tuNgay(LocalDateTime t) {\n        return t == null ? null : (root, q, cb) -> cb.greaterThanOrEqualTo(root.get(\"createdAt\"), t);\n    }\n    static Specification<Order> tenKhachChua(String kw) {\n        return !StringUtils.hasText(kw) ? null : (root, q, cb) ->\n                cb.like(cb.lower(root.join(\"customer\").get(\"name\")), \"%\" + kw.toLowerCase() + \"%\");\n    }\n}\n\n// lắp ghép không cần một dòng if\nSpecification<Order> spec = Specification.allOf(\n        OrderSpecs.trangThai(f.status()),\n        OrderSpecs.tuNgay(f.from()),\n        OrderSpecs.tenKhachChua(f.keyword()));\n\nPage<Order> kq = repo.findAll(spec, pageable);\n\n// danh sách trắng cho sắp xếp\nprivate static final Set<String> SORT_OK = Set.of(\"createdAt\", \"amount\", \"status\");"]
  },
  {
   question: "Auditing trong Spring Data JPA và các callback vòng đời của entity hoạt động ra sao?",
   answer: "AUDITING TỰ ĐỘNG: Spring Data điền sẵn bốn trường thông dụng nhờ AuditingEntityListener.\n- @CreatedDate, @LastModifiedDate: thời điểm tạo và sửa cuối.\n- @CreatedBy, @LastModifiedBy: ai tạo, ai sửa — lấy từ một bean AuditorAware<T> mà bạn cung cấp, thường đọc từ SecurityContextHolder.\nBật bằng @EnableJpaAuditing, và entity phải có @EntityListeners(AuditingEntityListener.class). Thường gom bốn trường này vào một lớp @MappedSuperclass để mọi entity kế thừa.\nLƯU Ý: các trường này chỉ được điền khi đi qua vòng đời JPA — UPDATE hàng loạt bằng @Modifying hay SQL thuần sẽ KHÔNG cập nhật chúng. Nếu cần bảo đảm tuyệt đối thì đặt thêm trigger hoặc DEFAULT ở phía DB.\n\nCÁC CALLBACK VÒNG ĐỜI (chuẩn JPA): @PrePersist, @PostPersist, @PreUpdate, @PostUpdate, @PreRemove, @PostRemove, @PostLoad. Đặt trực tiếp trên method của entity hoặc gom vào một lớp listener riêng.\n\nBỐN QUY TẮC AN TOÀN KHI DÙNG CALLBACK — vi phạm là gặp lỗi rất khó lần:\n1. KHÔNG gọi EntityManager hay repository bên trong callback. Bạn đang ở GIỮA quá trình flush; thêm truy vấn ở đó dẫn tới hành vi không xác định, thậm chí ConcurrentModificationException trong ActionQueue.\n2. KHÔNG làm việc nặng hay gọi mạng — nó nằm trong transaction và kéo dài thời gian giữ khóa.\n3. @PreUpdate chỉ chạy khi Hibernate THẬT SỰ phát hiện thay đổi; không đổi gì thì không có UPDATE và cũng không có callback.\n4. Callback không chạy với truy vấn hàng loạt và không chạy với native query.\n\nCÁCH ĐÚNG CHO VIỆC PHỤ THUỘC BÊN NGOÀI: đừng nhét vào callback. Dùng sự kiện của Spring — phát ApplicationEvent rồi lắng nghe bằng @TransactionalEventListener(phase = AFTER_COMMIT). Như vậy việc phụ (gửi mail, đẩy Kafka, gọi API) chỉ chạy KHI transaction đã commit thành công, và lỗi của nó không kéo đổ nghiệp vụ chính. Đây cũng là nền tảng để triển khai mẫu Outbox.\n\nĐiểm ghi bàn: nêu quy tắc 'không gọi repository trong callback' và giới thiệu @TransactionalEventListener AFTER_COMMIT thay thế.",
   examples: ["@Configuration\n@EnableJpaAuditing(auditorAwareRef = \"auditorAware\")\nclass AuditConfig {\n    @Bean AuditorAware<String> auditorAware() {\n        return () -> Optional.ofNullable(SecurityContextHolder.getContext().getAuthentication())\n                .map(Authentication::getName);\n    }\n}\n\n@MappedSuperclass\n@EntityListeners(AuditingEntityListener.class)\npublic abstract class BaseEntity {\n    @CreatedDate  @Column(updatable = false) private Instant createdAt;\n    @LastModifiedDate private Instant updatedAt;\n    @CreatedBy    @Column(updatable = false) private String createdBy;\n    @LastModifiedBy private String updatedBy;\n}\n\n// việc phụ: CHỜ commit xong mới chạy\n@Transactional\npublic void thanhToan(Order o) {\n    repo.save(o);\n    events.publishEvent(new OrderPaidEvent(o.getId()));   // chỉ phát sự kiện\n}\n\n@TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT)\npublic void guiMail(OrderPaidEvent e) { mailService.send(e.orderId()); }"]
  },
  {
   question: "Ánh xạ quan hệ trong JPA: mappedBy, quan hệ hai chiều và @ManyToMany nên làm thế nào?",
   answer: "AI SỞ HỮU QUAN HỆ: phía SỞ HỮU là phía có cột KHÓA NGOẠI trong DB — thường là phía @ManyToOne. Phía kia đánh dấu mappedBy để nói 'tôi chỉ là ảnh phản chiếu, đừng ghi gì cả'. Hibernate CHỈ NHÌN vào phía sở hữu khi sinh SQL.\n\nHỆ QUẢ SỐ MỘT — LỖI PHỔ BIẾN NHẤT: bạn thêm phần tử vào collection phía mappedBy mà quên set phía sở hữu -> khóa ngoại vẫn null, dữ liệu không được lưu, và bạn ngồi tự hỏi vì sao. Giải pháp bắt buộc: viết METHOD TIỆN ÍCH đồng bộ CẢ HAI phía và luôn dùng nó thay cho add() trực tiếp.\n\nQUÊN mappedBy thì sao: Hibernate coi đó là hai quan hệ độc lập và tự tạo thêm một BẢNG NỐI ngoài dự kiến, hoặc sinh thừa câu UPDATE khóa ngoại. Đây là lỗi ánh xạ hay gặp.\n\nCÓ NÊN LÀM HAI CHIỀU KHÔNG: chỉ khi bạn THỰC SỰ cần duyệt theo chiều đó. Quan hệ hai chiều làm phức tạp equals/hashCode, gây vòng lặp khi serialize JSON (phải dùng @JsonIgnore hoặc DTO), và dễ nạp thừa. Một chiều @ManyToOne thường là đủ; muốn lấy danh sách con thì viết truy vấn repository.\n\n@ManyToMany — LỜI KHUYÊN LÀ ĐỪNG DÙNG TRỰC TIẾP:\n1. Bảng nối do JPA quản lý không chứa thêm được cột nào. Thực tế gần như LUÔN phát sinh nhu cầu thêm cột (ngày gán, người gán, trạng thái) và lúc đó bạn phải tái cấu trúc toàn bộ.\n2. Với List, Hibernate xử lý cập nhật rất kém: sửa một phần tử nó XÓA SẠCH các dòng nối rồi CHÈN LẠI toàn bộ. Dùng Set thì đỡ hơn nhiều.\n3. CascadeType.REMOVE trên @ManyToMany là cực kỳ nguy hiểm — xóa một bài viết có thể xóa luôn cái thẻ (tag) mà hàng nghìn bài khác đang dùng. Chỉ dùng PERSIST và MERGE.\nCÁCH ĐÚNG: tách bảng nối thành một ENTITY riêng với khóa chính tổng hợp hoặc id riêng, rồi map thành hai quan hệ @ManyToOne. Rõ ràng, mở rộng được, kiểm soát được truy vấn.\n\nGHI CHÚ VỀ CASCADE VÀ orphanRemoval: CascadeType.ALL trên quan hệ tới cha là sai lầm hay gặp (xóa một đơn hàng kéo theo xóa cả khách hàng). orphanRemoval = true nghĩa là phần tử bị gỡ khỏi collection sẽ bị XÓA khỏi DB — chỉ đúng khi quan hệ thực sự là 'sở hữu trọn vẹn' như Order với OrderItem.\n\nĐiểm ghi bàn: viết được method đồng bộ hai chiều và giải thích vì sao nên tách bảng nối thành entity.",
   examples: ["@Entity\nclass Order {\n    @OneToMany(mappedBy = \"order\", cascade = CascadeType.ALL, orphanRemoval = true)\n    private List<OrderItem> items = new ArrayList<>();\n\n    // LUÔN dùng method này thay vì items.add()\n    public void themItem(OrderItem it) { items.add(it); it.setOrder(this); }\n    public void xoaItem(OrderItem it) { items.remove(it); it.setOrder(null); }\n}\n\n@Entity\nclass OrderItem {\n    @ManyToOne(fetch = FetchType.LAZY)\n    @JoinColumn(name = \"order_id\")     // phía SỞ HỮU — có khóa ngoại\n    private Order order;\n}\n\n// thay cho @ManyToMany: bảng nối là entity thật\n@Entity\nclass PostTag {\n    @EmbeddedId private PostTagId id;\n    @ManyToOne @MapsId(\"postId\") private Post post;\n    @ManyToOne @MapsId(\"tagId\")  private Tag tag;\n    private Instant assignedAt;        // cột phụ — @ManyToMany không làm được\n    private String assignedBy;\n}"]
  }
 ]
},
{
 topic: "Java hiện đại (Java 8 → 21)",
 items: [
  {
   question: "Stream API hoạt động thế nào bên trong? Lazy, short-circuit và pipeline nghĩa là gì?",
   answer: "Stream KHÔNG phải là một cấu trúc lưu dữ liệu — nó là một ĐƯỜNG ỐNG XỬ LÝ đọc từ nguồn (collection, mảng, generator) và chạy MỘT LẦN duy nhất (dùng lại sẽ ném IllegalStateException).\n\nHAI LOẠI THAO TÁC:\n- TRUNG GIAN (map, filter, sorted, distinct, peek, limit): LƯỜI — chỉ dựng cấu hình pipeline, KHÔNG chạy gì cả. Viết một chuỗi map/filter mà không có thao tác cuối thì tuyệt đối không có gì xảy ra.\n- CUỐI (collect, forEach, reduce, count, findFirst, anyMatch, toList): kích hoạt toàn bộ pipeline.\n\nCƠ CHẾ QUAN TRỌNG NHẤT — DUYỆT THEO CHIỀU DỌC: nhiều người tưởng stream chạy hết filter cho cả danh sách rồi mới chạy map. Thực tế MỖI PHẦN TỬ đi qua TOÀN BỘ chuỗi thao tác rồi mới tới phần tử tiếp theo. Nhờ vậy findFirst() có thể dừng ngay sau phần tử đầu tiên thỏa mãn mà không đụng tới các phần tử còn lại. Đây gọi là short-circuit, có ở anyMatch, allMatch, noneMatch, findFirst, findAny, limit.\n\nTHAO TÁC CÓ TRẠNG THÁI (sorted, distinct): buộc phải gom hết dữ liệu trước khi cho ra phần tử đầu tiên, nên chúng chặn đứng lợi ích của lazy. Đặt filter TRƯỚC sorted luôn tốt hơn đặt sau.\n\nreduce vs collect: reduce dùng cho phép gộp BẤT BIẾN (cộng, nhân, min); collect dùng cho gộp CÓ THAY ĐỔI vào một vùng chứa (list, map, chuỗi). Dùng reduce với String để nối chuỗi là bẫy hiệu năng O(n²) — hãy dùng Collectors.joining.\n\nCÁC ĐIỂM THỰC CHIẾN:\n- Dùng IntStream/LongStream/DoubleStream cho số để tránh boxing.\n- Collectors.toMap NÉM IllegalStateException khi có khóa TRÙNG — luôn truyền hàm gộp thứ ba. Nó cũng ném NPE nếu giá trị là null (khác với HashMap thường).\n- peek chỉ nên dùng để gỡ lỗi; một số phiên bản JDK còn được phép bỏ qua nó khi tối ưu.\n- Không sửa collection nguồn trong lúc stream đang chạy.\n- Vòng lặp for thường vẫn dễ đọc và nhanh hơn cho việc đơn giản — stream không phải luôn tốt hơn.\n\nĐiểm ghi bàn: giải thích duyệt theo chiều dọc và bẫy Collectors.toMap khóa trùng.",
   examples: ["// KHÔNG có gì chạy — thiếu thao tác cuối\nlist.stream().filter(x -> x > 10).map(this::bien_doi);\n\n// duyệt theo chiều dọc: chỉ xử lý tới khi tìm thấy\nOptional<User> u = users.stream()\n        .filter(x -> x.isActive())     // chạy xen kẽ với map, không phải trước\n        .map(this::nangCap)\n        .findFirst();                  // dừng ngay khi có kết quả\n\n// BẪY: khóa trùng -> IllegalStateException\nMap<String, User> m = users.stream()\n        .collect(Collectors.toMap(User::getEmail, u2 -> u2, (a, b) -> b));  // hàm gộp\n\n// nhóm và thống kê\nMap<Status, List<Order>> theoTrangThai = orders.stream()\n        .collect(Collectors.groupingBy(Order::getStatus));\n\nMap<Status, BigDecimal> tong = orders.stream()\n        .collect(Collectors.groupingBy(Order::getStatus,\n                 Collectors.reducing(BigDecimal.ZERO, Order::getAmount, BigDecimal::add)));\n\n// tránh boxing\nint tongTuoi = users.stream().mapToInt(User::getAge).sum();"]
  },
  {
   question: "parallelStream có những cạm bẫy nào? Khi nào thật sự nên dùng?",
   answer: "CƠ CHẾ: parallelStream chia dữ liệu bằng Spliterator rồi chạy trên ForkJoinPool.commonPool(). Kích thước pool mặc định là SỐ LÕI TRỪ 1 và pool này DÙNG CHUNG cho toàn JVM.\n\nBỐN CẠM BẪY:\n\n1. DÙNG CHUNG COMMON POOL: mọi parallelStream trong ứng dụng, kể cả trong thư viện bên thứ ba, đều chen nhau trên cùng một pool. Một tác vụ chậm làm nghẽn tất cả phần còn lại. Muốn cách ly thì phải submit vào ForkJoinPool riêng.\n\n2. TUYỆT ĐỐI KHÔNG DÙNG CHO TÁC VỤ CHẶN: gọi HTTP, truy vấn DB, đọc file trong parallelStream sẽ chiếm giữ luồng của common pool mà không làm gì ngoài chờ — và có thể làm đói toàn bộ ứng dụng. Song song hóa IO phải dùng ExecutorService riêng hoặc virtual thread.\n\n3. CHI PHÍ CHIA VÀ GỘP: với dữ liệu nhỏ hoặc thao tác rẻ, chi phí chia việc và gộp kết quả lớn hơn phần tiết kiệm được — chạy song song lại CHẬM HƠN. Ngưỡng hòa vốn thường vào khoảng vài chục nghìn phần tử với thao tác nhẹ.\n\n4. NGUỒN DỮ LIỆU KHÔNG CHIA ĐỀU ĐƯỢC: ArrayList và mảng chia rất tốt (biết chỉ số). LinkedList, Stream.iterate hay dòng đọc từ file chia rất tệ, gần như không có lợi ích.\n\n5. TÁC DỤNG PHỤ VÀ THỨ TỰ: lambda phải KHÔNG có trạng thái chia sẻ. Gom kết quả vào một ArrayList thường trong forEach là hỏng dữ liệu ngay (dùng collect để nó tự lo). forEach không bảo đảm thứ tự — cần thứ tự thì dùng forEachOrdered, nhưng cái đó lại xóa gần hết lợi ích.\n\nKHI NÀO NÊN DÙNG: dữ liệu LỚN (thường trên vài chục nghìn phần tử), nằm trong cấu trúc chia đều được, thao tác TỐN CPU thuần (tính toán, mã hóa, phân tích chuỗi), không có tác dụng phụ, và bạn ĐÃ ĐO thấy nhanh hơn. Không đo thì đừng dùng.\n\nĐiểm ghi bàn: nêu 'không dùng cho tác vụ chặn' và vấn đề common pool dùng chung.",
   examples: ["// SAI — chặn IO trên common pool, có thể làm đói cả ứng dụng\nids.parallelStream().map(id -> httpClient.get(id)).toList();\n\n// ĐÚNG — pool riêng cho IO\nExecutorService pool = Executors.newFixedThreadPool(20);\nList<CompletableFuture<Res>> fs = ids.stream()\n        .map(id -> CompletableFuture.supplyAsync(() -> httpClient.get(id), pool))\n        .toList();\n\n// hoặc Java 21: virtual thread, hợp nhất cho IO\ntry (var ex = Executors.newVirtualThreadPerTaskExecutor()) {\n    ids.forEach(id -> ex.submit(() -> httpClient.get(id)));\n}\n\n// SAI — tác dụng phụ vào collection không an toàn\nList<String> kq = new ArrayList<>();\nbig.parallelStream().forEach(kq::add);        // hỏng dữ liệu\n\n// ĐÚNG\nList<String> kq2 = big.parallelStream().map(this::xuLy).toList();\n\n// cách ly common pool khi thật sự cần\nForkJoinPool rieng = new ForkJoinPool(4);\nrieng.submit(() -> data.parallelStream().map(this::tinhNang).toList()).get();"]
  },
  {
   question: "Record là gì? Khi nào dùng record thay cho class và nó có giới hạn gì?",
   answer: "RECORD (chính thức từ Java 16) là 'vật mang dữ liệu trong suốt': bạn khai báo các thành phần, trình biên dịch sinh sẵn tất cả phần còn lại.\n\nTRÌNH BIÊN DỊCH TỰ SINH:\n- Các field private final.\n- Constructor đầy đủ tham số (canonical constructor).\n- Method truy cập tên TRÙNG với tên thành phần — là name() chứ KHÔNG phải getName(). Đây là chi tiết hay gây bất ngờ khi tích hợp với thư viện dựa vào quy ước JavaBean.\n- equals và hashCode dựa trên TẤT CẢ thành phần.\n- toString dễ đọc.\n\nĐẶC ĐIỂM BẤT BIẾN: record là final, không kế thừa được lớp khác (nó đã ngầm kế thừa java.lang.Record), không có field instance nào ngoài các thành phần đã khai. Nhưng nó CÀI ĐẶT được interface, có static field, static method và method thường.\n\nCOMPACT CONSTRUCTOR: viết constructor không có danh sách tham số để thêm kiểm tra hợp lệ hoặc chuẩn hóa. Đây cũng là nơi phải SAO CHÉP PHÒNG THỦ nếu thành phần là collection hay mảng — record chỉ bất biến NÔNG, truyền vào một ArrayList thì bên ngoài vẫn sửa được.\n\nDÙNG TỐT CHO: DTO, request/response của API, value object (Money, Range, Coordinate), khóa tổng hợp của Map, kiểu trả về nhiều giá trị, kết quả của truy vấn projection, và các nhánh dữ liệu trong sealed interface.\n\nKHÔNG DÙNG CHO: entity JPA. Lý do rất cụ thể — JPA cần constructor rỗng, cần sửa được field (để gán id sau khi insert), cần tạo proxy bằng lớp con, và equals theo mọi trường thì xung đột với ngữ nghĩa định danh của entity. Record vi phạm cả bốn.\n\nGHI CHÚ TÍCH HỢP: Jackson hỗ trợ record từ phiên bản 2.12; Bean Validation đặt annotation trên thành phần vẫn chạy được (có thể cần chỉ rõ vị trí bằng @field: hoặc dùng cấu hình phù hợp tùy framework).\n\nĐiểm ghi bàn: nêu lý do cụ thể vì sao record không làm entity JPA được.",
   examples: ["public record Money(BigDecimal amount, String currency) implements Comparable<Money> {\n    // compact constructor: kiểm tra + chuẩn hóa\n    public Money {\n        Objects.requireNonNull(amount);\n        if (amount.signum() < 0) throw new IllegalArgumentException(\"Số tiền âm\");\n        currency = currency.toUpperCase();\n    }\n    public Money cong(Money o) {\n        if (!currency.equals(o.currency)) throw new IllegalArgumentException(\"Khác loại tiền\");\n        return new Money(amount.add(o.amount), currency);\n    }\n    @Override public int compareTo(Money o) { return amount.compareTo(o.amount); }\n}\n\nMoney m = new Money(new BigDecimal(\"100\"), \"vnd\");\nm.amount();     // KHÔNG phải getAmount()\n\n// bất biến NÔNG -> phải tự sao chép\nrecord Basket(List<String> items) {\n    Basket { items = List.copyOf(items); }\n}\n\n// hợp làm khóa Map (equals/hashCode có sẵn, bất biến)\nrecord CacheKey(Long userId, String feature) {}\nMap<CacheKey, Result> cache = new HashMap<>();"]
  },
  {
   question: "Sealed class và pattern matching trong switch dùng thế nào?",
   answer: "SEALED (Java 17): giới hạn CHÍNH XÁC những lớp nào được phép kế thừa, khai báo bằng permits. Mỗi lớp con phải tự nhận một trong ba trạng thái: final (không cho kế thừa tiếp), sealed (giới hạn tiếp), hoặc non-sealed (mở lại).\n\nGIÁ TRỊ THẬT SỰ: trình biên dịch biết ĐẦY ĐỦ tập lớp con nên nó kiểm tra được switch có VÉT CẠN hay chưa. Thiếu một nhánh là lỗi biên dịch. Nhờ vậy khi bạn thêm một loại mới, trình biên dịch sẽ chỉ ra TẤT CẢ những chỗ cần cập nhật — thay vì phát hiện ra lúc chạy bằng một exception ở nhánh default. Đây là kiểu dữ liệu tổng (sum type) mà các ngôn ngữ hàm đã có từ lâu.\n\nPATTERN MATCHING FOR SWITCH (chính thức Java 21) kết hợp với sealed cho phép rẽ nhánh theo kiểu, tự ép kiểu, thêm điều kiện bảo vệ bằng when, và giải cấu trúc record lồng nhau. Đây là cách thay thế sạch sẽ cho chuỗi if-instanceof dài lê thê.\n\nBA ĐIỂM PHẢI NHỚ:\n1. switch trên kiểu tham chiếu phải xử lý null tường minh bằng nhãn 'case null' — nếu không nó ném NullPointerException (giữ tương thích với hành vi cũ của switch).\n2. Thứ tự các case QUAN TRỌNG: nhánh tổng quát hơn phải nằm SAU, nếu không nó che mất nhánh cụ thể và trình biên dịch báo lỗi.\n3. Có switch vét cạn rồi thì đừng thêm default — thêm default là bạn tự tắt luôn cơ chế kiểm tra vét cạn quý giá kia.\n\nKẾT HỢP VỚI RECORD: sealed interface + các record cài đặt nó là công thức mô hình hóa 'một trong các trường hợp' rất mạnh — trạng thái đơn hàng, kết quả thành công/thất bại, các loại sự kiện, cây biểu thức.\n\nCÒN CÓ: enhanced instanceof (Java 16) cho phép viết 'if (o instanceof Order od)' và dùng ngay biến od mà không cần ép kiểu thủ công.\n\nĐiểm ghi bàn: nhấn mạnh 'không viết default để giữ kiểm tra vét cạn' — dấu hiệu của người dùng tính năng này thật sự.",
   examples: ["public sealed interface KetQua permits ThanhCong, ThatBai, DangCho {}\n\npublic record ThanhCong(String maGiaoDich, BigDecimal soTien) implements KetQua {}\npublic record ThatBai(String maLoi, String moTa) implements KetQua {}\npublic record DangCho(Instant thuLai) implements KetQua {}\n\n// switch VÉT CẠN — thêm nhánh mới vào sealed là lỗi biên dịch ở đây\nString mo_ta = switch (kq) {\n    case ThanhCong t when t.soTien().compareTo(TRIEU) > 0 ->\n            \"Giao dịch lớn \" + t.maGiaoDich();\n    case ThanhCong t  -> \"Thành công \" + t.maGiaoDich();\n    case ThatBai f    -> \"Lỗi \" + f.maLoi();\n    case DangCho d    -> \"Chờ tới \" + d.thuLai();\n    case null         -> \"Không có kết quả\";      // phải xử lý tường minh\n    // KHÔNG viết default -> giữ được kiểm tra vét cạn\n};\n\n// giải cấu trúc record lồng nhau (Java 21)\nif (obj instanceof ThanhCong(String ma, BigDecimal tien) && tien.signum() > 0) {\n    log.info(\"{} = {}\", ma, tien);\n}"]
  },
  {
   question: "Virtual thread (Java 21) là gì? Nó thay đổi cách viết ứng dụng backend ra sao?",
   answer: "VẤN ĐỀ CŨ: mỗi luồng nền tảng (platform thread) tương ứng một luồng của hệ điều hành, tốn khoảng 1MB stack và việc chuyển ngữ cảnh khá đắt. Vì thế ta chỉ chạy được vài nghìn luồng, buộc phải dùng pool. Với ứng dụng backend chủ yếu CHỜ IO, các luồng trong pool ngồi không chờ mạng trong khi request khác xếp hàng — đó là lý do người ta phải chuyển sang lập trình bất đồng bộ/reactive, đổi lấy code khó đọc và khó gỡ lỗi.\n\nVIRTUAL THREAD: luồng do JVM quản lý, KHÔNG gắn cứng vào luồng hệ điều hành. Tạo một cái tốn vài trăm byte, tạo hàng TRIỆU cái là bình thường. Khi luồng ảo chạm một thao tác chặn (đọc socket, chờ khóa), JVM GỠ nó khỏi luồng mang (carrier thread) và cho luồng mang phục vụ luồng ảo khác; khi dữ liệu về thì gắn lại. Toàn bộ chuyện này trong suốt với code của bạn.\n\nÝ NGHĨA LỚN NHẤT: bạn quay lại viết code CHẶN, TUẦN TỰ, DỄ ĐỌC — một luồng cho một request — mà vẫn đạt mức mở rộng của mô hình bất đồng bộ. Stack trace lại đầy đủ và có ý nghĩa; gỡ lỗi và profiling trở lại bình thường.\n\nBA ĐIỂM PHẢI BIẾT:\n1. ĐỪNG POOL luồng ảo. Chúng rẻ tới mức nên tạo mới cho mỗi tác vụ. Executors.newVirtualThreadPerTaskExecutor() là mẫu chuẩn. Pool giờ chỉ còn để GIỚI HẠN tài nguyên (như số kết nối DB) và việc đó nên làm bằng Semaphore.\n2. PINNING: nếu luồng ảo chặn khi đang ở trong khối synchronized hoặc trong lời gọi native, nó KHÔNG gỡ ra được và chiếm luôn luồng mang — mất hết lợi ích. Cách chữa: thay synchronized bằng ReentrantLock ở các đoạn có IO. (Java 24 đã xử lý phần lớn vấn đề này, nhưng với Java 21 thì vẫn cần lưu ý.)\n3. ThreadLocal vẫn chạy nhưng với hàng triệu luồng thì tốn bộ nhớ — Scoped Values là hướng thay thế đang được phát triển.\n\nNÚT CỔ CHAI CHUYỂN CHỖ: khi luồng không còn là giới hạn, thứ giới hạn tiếp theo là CONNECTION POOL của DB và bản thân DB. Bật virtual thread mà giữ nguyên pool 10 kết nối thì thông lượng không tăng bao nhiêu — phải nhìn lại toàn bộ chuỗi.\n\nTRONG SPRING BOOT 3.2+: chỉ cần một dòng cấu hình là Tomcat chuyển sang dùng luồng ảo cho mỗi request.\n\nĐiểm ghi bàn: nêu vấn đề pinning và việc nút cổ chai dịch sang connection pool.",
   examples: ["# Spring Boot 3.2+ — một dòng\nspring.threads.virtual.enabled=true\n\n// tạo thoải mái, KHÔNG pool\ntry (var ex = Executors.newVirtualThreadPerTaskExecutor()) {\n    for (var id : ids) ex.submit(() -> goiApi(id));   // 100.000 tác vụ vẫn ổn\n}\n\n// giới hạn tài nguyên bằng semaphore, không phải bằng pool\nprivate final Semaphore gioiHanDb = new Semaphore(20);\nvoid truyVan() throws InterruptedException {\n    gioiHanDb.acquire();\n    try { repo.findAll(); } finally { gioiHanDb.release(); }\n}\n\n// PINNING: tránh synchronized quanh đoạn có IO\n// synchronized void xau() { httpClient.get(...); }     // ghim luồng mang\nprivate final ReentrantLock lock = new ReentrantLock();\nvoid tot() {\n    lock.lock();\n    try { httpClient.get(...); } finally { lock.unlock(); }\n}\n\n# phát hiện pinning\n-Djdk.tracePinnedThreads=full"]
  },
  {
   question: "java.time thay thế Date/Calendar thế nào? Chọn kiểu nào cho trường hợp nào?",
   answer: "VÌ SAO BỎ Date VÀ Calendar: chúng CÓ THỂ THAY ĐỔI (không an toàn đa luồng), tháng đếm TỪ 0 (tháng 1 là 0 — nguồn lỗi bất tận), năm của Date tính từ 1900, SimpleDateFormat không an toàn đa luồng nhưng lại hay bị khai báo làm biến static, và API thì thiếu nhất quán. java.time (Java 8, dựa trên Joda-Time) sửa toàn bộ: mọi lớp đều BẤT BIẾN và an toàn đa luồng.\n\nCHỌN KIỂU NÀO:\n- LocalDate: chỉ ngày, không giờ, không múi giờ. Dùng cho ngày sinh, ngày đáo hạn, ngày lễ.\n- LocalTime: chỉ giờ trong ngày. Giờ mở cửa.\n- LocalDateTime: ngày và giờ nhưng KHÔNG có múi giờ — nghĩa là nó KHÔNG xác định một điểm thật trên trục thời gian. Đây là lỗi chọn kiểu phổ biến nhất: dùng nó để lưu thời điểm xảy ra sự kiện.\n- Instant: một ĐIỂM tuyệt đối trên trục thời gian (số giây từ mốc epoch, theo UTC). ĐÂY LÀ KIỂU ĐÚNG cho createdAt, updatedAt, thời điểm giao dịch, log.\n- ZonedDateTime: thời điểm cộng múi giờ đầy đủ, hiểu quy tắc giờ mùa hè. Dùng khi phải hiển thị hoặc tính toán theo giờ địa phương của người dùng.\n- OffsetDateTime: thời điểm cộng độ lệch cố định (+07:00). Thường dùng để trao đổi qua API và lưu DB.\n- Duration (khoảng thời gian theo giây/nano) và Period (theo ngày/tháng/năm) — phân biệt rõ: cộng 1 tháng vào 31/1 cho ra 28/2, còn cộng 30 ngày lại ra kết quả khác.\n\nQUY TẮC VÀNG VỀ MÚI GIỜ: LƯU MỌI THỜI ĐIỂM Ở UTC (Instant hoặc timestamptz), CHỈ ĐỔI SANG GIỜ ĐỊA PHƯƠNG Ở TẦNG HIỂN THỊ. Không bao giờ dựa vào múi giờ mặc định của máy chủ — nó khác nhau giữa máy dev, container và môi trường chạy thật, gây ra lớp lỗi chỉ xuất hiện trên production.\n\nGHI CHÚ THỰC CHIẾN:\n- Với JPA, java.time được hỗ trợ sẵn từ JPA 2.2; chọn cột timestamptz của PostgreSQL cho Instant.\n- DateTimeFormatter LÀ an toàn đa luồng (khác hẳn SimpleDateFormat) nên khai báo static final thoải mái.\n- Trong test, tiêm một Clock thay vì gọi Instant.now() trực tiếp — nhờ đó test được các mốc thời gian mà không cần chờ.\n\nĐiểm ghi bàn: phân biệt được LocalDateTime và Instant, cùng quy tắc lưu UTC.",
   examples: ["// SAI: LocalDateTime không xác định điểm thật trên trục thời gian\n@Column private LocalDateTime createdAt;\n\n// ĐÚNG\n@Column(columnDefinition = \"timestamptz\") private Instant createdAt;\n\n// đổi sang giờ người dùng chỉ ở tầng hiển thị\nZonedDateTime hienThi = createdAt.atZone(ZoneId.of(\"Asia/Ho_Chi_Minh\"));\n\n// Period vs Duration\nLocalDate.of(2026, 1, 31).plus(Period.ofMonths(1));   // 2026-02-28\nInstant.now().plus(Duration.ofDays(30));\n\n// formatter an toàn đa luồng\nprivate static final DateTimeFormatter FMT =\n        DateTimeFormatter.ofPattern(\"dd/MM/yyyy HH:mm\").withZone(ZoneId.of(\"Asia/Ho_Chi_Minh\"));\n\n// tiêm Clock để test được\n@Bean Clock clock() { return Clock.systemUTC(); }\n\nclass Service {\n    private final Clock clock;\n    void tao() { order.setCreatedAt(Instant.now(clock)); }\n}\n// trong test: Clock.fixed(Instant.parse(\"2026-01-01T00:00:00Z\"), ZoneOffset.UTC)"]
  },
  {
   question: "var, text block, switch expression và các cải tiến cú pháp từ Java 10 tới 21 gồm những gì?",
   answer: "var (Java 10) — SUY LUẬN KIỂU CHO BIẾN CỤC BỘ. Đây KHÔNG phải kiểu động: kiểu vẫn được chốt lúc biên dịch và không đổi được. Chỉ dùng được cho biến cục bộ, biến đếm vòng lặp, biến trong try-with-resources; KHÔNG dùng được cho field, tham số method hay kiểu trả về.\nDÙNG KHI kiểu đã hiển nhiên từ vế phải (var list = new ArrayList<Order>()) hoặc khi kiểu quá dài dòng. ĐỪNG DÙNG khi nó xóa mất thông tin quan trọng (var x = service.xuLy() thì người đọc chẳng biết x là gì) — nguyên tắc là đọc dễ hơn thì dùng, khó hơn thì thôi.\n\nTEXT BLOCK (Java 15) — chuỗi nhiều dòng đặt trong ba dấu nháy kép. Trình biên dịch tự bỏ phần thụt lề chung, nên SQL và JSON trong code trông đúng như thật mà không cần nối chuỗi hay ký tự thoát. Ký tự \\ ở cuối dòng nghĩa là nối tiếp không xuống dòng; \\s giữ lại khoảng trắng cuối dòng.\n\nSWITCH EXPRESSION (Java 14) — switch giờ TRẢ VỀ GIÁ TRỊ. Dùng mũi tên -> nên KHÔNG rơi tầng (không cần break, hết một lớp lỗi kinh điển), và khi switch trên enum thì trình biên dịch bắt buộc phải vét cạn. Cần nhiều dòng thì dùng khối và từ khóa yield.\n\nCÁC BỔ SUNG KHÁC ĐÁNG NHỚ:\n- Java 11: String.isBlank, strip, lines, repeat; Files.readString/writeString; HttpClient chuẩn thay cho thư viện ngoài; chạy trực tiếp file .java không cần biên dịch riêng.\n- Java 16: enhanced instanceof (ép kiểu tự động), Stream.toList().\n- Java 17 (LTS): sealed class, text block chính thức.\n- Java 21 (LTS): pattern matching cho switch, record pattern, virtual thread, sequenced collections (getFirst/getLast/reversed thống nhất cho List, Deque, LinkedHashMap — cuối cùng cũng có API chung).\n\nGỢI Ý PHIÊN BẢN: Java 8 vẫn còn rất phổ biến trong hệ thống cũ, nhưng dự án mới nên bắt đầu từ 17 hoặc 21 (đều là bản LTS). Nhảy từ 8 lên 17 thì việc phải xử lý chủ yếu là các API đã bị gỡ (javax.xml.bind, CORBA), việc đổi javax sang jakarta khi lên Spring Boot 3, và các cờ GC cũ không còn dùng được.\n\nĐiểm ghi bàn: nói rõ 'var không phải kiểu động' và nêu switch expression loại bỏ lỗi rơi tầng.",
   examples: ["// var — dùng khi kiểu đã hiển nhiên\nvar orders = new ArrayList<Order>();          // rõ\nvar x = service.xuLy();                        // mờ — nên ghi rõ kiểu\n\n// text block\nvar sql = \"\"\"\n        SELECT o.id, o.amount, c.name\n        FROM orders o\n        JOIN customers c ON c.id = o.customer_id\n        WHERE o.status = ?\n          AND o.created_at >= ?\n        \"\"\";\n\n// switch expression — không rơi tầng, vét cạn với enum\nint ngay = switch (thang) {\n    case FEB -> namNhuan ? 29 : 28;\n    case APR, JUN, SEP, NOV -> 30;\n    default -> 31;\n};\n\n// khối nhiều dòng dùng yield\nString mucDo = switch (code) {\n    case 200, 201 -> \"OK\";\n    default -> {\n        log.warn(\"mã lạ {}\", code);\n        yield \"KHÔNG RÕ\";\n    }\n};\n\n// sequenced collections (Java 21)\nlist.getFirst(); list.getLast(); list.reversed();"]
  },
  {
   question: "Functional interface, lambda và method reference: các interface có sẵn và cách viết API dùng hàm?",
   answer: "FUNCTIONAL INTERFACE là interface có ĐÚNG MỘT method trừu tượng (default và static không tính). Chỉ những interface như vậy mới gán được lambda. @FunctionalInterface là annotation tùy chọn nhưng nên có: nó khiến trình biên dịch báo lỗi nếu ai đó vô tình thêm method thứ hai.\n\nSÁU INTERFACE CỐT LÕI trong java.util.function:\n- Function<T,R>: nhận T trả R. Ghép được bằng andThen (chạy sau) và compose (chạy trước).\n- BiFunction<T,U,R>: hai đầu vào.\n- Predicate<T>: trả boolean. Ghép bằng and, or, negate.\n- Consumer<T>: nhận, không trả gì. Ghép bằng andThen.\n- Supplier<T>: không nhận, trả T. Nền tảng của việc TÍNH LƯỜI.\n- UnaryOperator<T> / BinaryOperator<T>: dạng rút gọn khi vào và ra cùng kiểu.\nCác biến thể cho số nguyên thủy (IntPredicate, ToIntFunction, IntUnaryOperator...) tồn tại để TRÁNH BOXING — trong vòng lặp nóng thì khác biệt là đáng kể.\n\nBỐN DẠNG METHOD REFERENCE:\n1. Static: Integer::parseInt.\n2. Trên một object cụ thể: System.out::println.\n3. Trên một object bất kỳ của một kiểu: String::toUpperCase (tham số đầu trở thành người nhận).\n4. Constructor: ArrayList::new.\n\nBA ĐIỂM CẦN LƯU Ý:\n1. Lambda chỉ bắt được biến effectively final (xem phần Java Core chuyên sâu).\n2. Lambda KHÔNG ném được checked exception nếu interface không khai báo — đây là nỗi đau khi dùng IO trong stream. Cách xử lý: bọc bằng một interface riêng có throws, hoặc gói thành RuntimeException, hoặc dùng thư viện tiện ích.\n3. Trong lambda, this trỏ về đối tượng bao ngoài, khác với anonymous class.\n\nTHIẾT KẾ API DÙNG HÀM: nhận Supplier thay vì giá trị khi việc tính toán tốn kém và có thể không cần tới; nhận Function để cho phép người dùng tùy biến bước biến đổi; trả Optional thay vì null. Nhưng đừng lạm dụng — một interface nghiệp vụ có tên rõ ràng thường dễ đọc hơn Function<A, B> chung chung.\n\nĐiểm ghi bàn: nêu vấn đề checked exception trong lambda và lý do có các biến thể nguyên thủy.",
   examples: ["@FunctionalInterface\ninterface Validator<T> {\n    List<String> kiemTra(T obj);\n    default Validator<T> and(Validator<T> khac) {\n        return o -> Stream.concat(kiemTra(o).stream(), khac.kiemTra(o).stream()).toList();\n    }\n}\n\n// ghép predicate\nPredicate<Order> lonVaChuaTra = ((Predicate<Order>) o -> o.getAmount().compareTo(TRIEU) > 0)\n        .and(o -> o.getStatus() == PENDING);\n\n// tính lười bằng Supplier\npublic <T> T layHoacTinh(String key, Supplier<T> tinh) {\n    return (T) cache.computeIfAbsent(key, k -> tinh.get());   // chỉ tính khi thiếu\n}\n\n// checked exception trong lambda\n@FunctionalInterface\ninterface ThrowingFunction<T, R> { R apply(T t) throws Exception; }\n\nstatic <T, R> Function<T, R> boc(ThrowingFunction<T, R> f) {\n    return t -> {\n        try { return f.apply(t); }\n        catch (Exception e) { throw new RuntimeException(e); }\n    };\n}\npaths.stream().map(boc(Files::readString)).toList();\n\n// tránh boxing trong vòng lặp nóng\nIntUnaryOperator nhanDoi = x -> x * 2;   // thay cho Function<Integer,Integer>"]
  }
 ]
}
];
